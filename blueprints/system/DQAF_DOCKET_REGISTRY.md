# DQAF Docket Registry — 案卷总账

> **标准参考**: IEC 62740:2015 (Root Cause Analysis), ISO 31000:2018 (Risk Management), NTSB Party System (49 CFR Part 831.11)
> **用途**: 记录每次正式诊断的元数据总览。症状简述不超过 60 字，详细内容引用 CCT_LEDGER.md 条目。
> **格式约定**: 表格格式（仅元数据，字段简短）。因果链详细内容见 `CCT_LEDGER.md`，模式签名见 `ReB_PATTERN_INDEX.md`。

## 严重等级定义

| 等级 | 定义 | 适用流程 |
|------|------|---------|
| Sev 1 | 交易阻断、数据丢失、资金风险 | 完整 Agentic DQAF（DA/AR 双轨 + IC 裁决） |
| Sev 2 | 输出偏差、信号退化、实盘质量下降 | 完整 Agentic DQAF（DA/AR 双轨 + IC 裁决） |
| Sev 3 | 告警噪音、技术债务、非关键异常 | 简化流程（单人 DA，无需 AR 对抗） |
| Sev 4 | 外观、文档、非功能性 | 现有 Iron Law #8 协议即可 |

## 案卷索引

| Docket ID | 日期 | 严重等级 | 症状简述 | IC 裁决 | 关联 FIX |
|-----------|------|---------|---------|--------|----------|
| DQAF-20260819-005 | 2026-08-19 | Sev 3 | **TECH_DEBT-009 清偿 — unified mypy 236 错误/62 测试文件 (UNIFIED_TEST_DEBT_A3_LATENT)**. 全量 `python -m mypy core/ apps/ scripts/ tests/` (unified 模式, follow_imports 真实解析) 暴露 236 类型错误 — isolated 模式 (follow_imports=skip, pre-commit/baseline 使用) 不可见的**导入泛型债** (签名缺泛型参数/非 Any 化, 仅在统一解析时暴露), 横跨 62 测试文件. FIX-20260819-002 遗留 unused-ignore (test_tech_debt_010:88 `# type:ignore[call-arg]`) 在 isolated 模式触发 warn_unused_ignores → **cast-not-ignore 策略**: 直接 `# type: ignore` 会 unused-ignore 阻塞 commit, `cast(Any)` 是纯类型层 no-op 双模式 (isolated/unified) 均干净. CCT 3 层链 (症状→unified 暴露 isolated 不可见类型债→测试域 A3 潜伏债务 8/19 后清偿). BlastRadius: 零资金/零运行时影响 (纯类型层); XAU/BTC 实盘零波及. ReB: UNIFIED_TEST_DEBT_A3_LATENT. | **CLOSED** — FIX-20260819-005 (分 4 批 97 文件: `51f29006` 批1×22 + `c0822a6f` 批2×11 + `92672e13` 批3×41 + `5a42840b` 批4×23, push `b1f8a3aa..5a42840b`): cast-not-ignore 策略 — 仅 unified 可见错误全部改 `cast(...)` 纯类型层遮蔽 (零运行时行为改变); 高重构成本且零安全风险的孤立警告 `# type: ignore` + 明确注释; 回归锁全绿 (pre_commit_mypy 0 新增 / verify --quick / ruff clean / 分块 ~pytest 全量 PASS). 零运行时行为改变纪律全程 97 文件验证. | FIX-20260819-005 |
| DQAF-20260819-002 | 2026-08-19 | Sev 2 | **影子风暴 ZMQ 隔离缺陷 + 跨域串台 (The Storm Forensics, TECH_DEBT-010 清偿)**. 双裂缝: ① `service_container.py` 默认 `tcp://127.0.0.1:5556` (XAU) 兜底 — 批量/回测/阴影进程连真 ZMQ 桥 → rejected 风暴; ② `dispatch_modify_trail`/close 路径漏传 per-symbol endpoint → BTC 命令落 XAU 桥 (7/20-8/4 BTC 227 条 modify_sltp rejected, 10 条混入主 journal data/ **跨域污染 XAU 账本**, magic 90460/90430). 8/6 缓解 (FIX-20260806-006) 只短路不除根 → **8/6-8/7 复发 3 次 140 条** (全月风暴特征 symbol=XAUUSD 缺c + `message_` 前缀 + magic=null = 281 条). **零成交污染** (golden_master 8/6-8/7 = 0). CCT 3 层链 (症状→非 --live-dispatch 进程未短路隔离+默认端口兜底→endpoint 漏传→多品种架构无显式注入契约). BlastRadius: XAU 主 journal 被 BTC rejected 污染 (SSOT 域污染, 训练/审计读脏); BTC 零资金损失 (227 请求全被桥拒收; **3 TRUE EXPOSURE** 短命微仓 4287199887/4331501668/4335847505 ≈-0.6~-1.8 USD, MT5 SL/TP 兜底). ReB: CROSS_ASSET_JOURNAL_CONTAMINATION + ZMQ_DEFAULT_PORT_FALLBACK. | **CLOSED** — FIX-20260819-002 (投委会雷霆裁决 5 执行令, 8/19 解封即清偿): **A Shadow Veto** — `bootstrap_v9.py` 影子容器遇生产网络适配器 (mt5_zmq 等) → `DataIntegrityError` 硬宕机, 不短路不告警直接死 (杜绝批量/回测连真桥); **B Journal Firewall** — `mt5_bridge_worker._append_journal` 唯一写盘 chokepoint 前域判定 (XAU↔XAUUSD(c) / BTC↔BTCUSDc, `--journal-domain`/`--default-symbol` 派生, 启动打印 firewall_armed/disarmed), 跨域记录写 `cross_domain_warnings.jsonl` **绝不进 SSOT**; **C Death of Defaults** — service_container/live_launcher 无 endpoint fail-fast (废除 5556 兜底), zmq_adapter 构造必传 endpoint, 全 dispatch 调用面 (open + modify C5 + 7 close 点) 补 per-symbol endpoint; **D 风险敞口核验先行** — 227 被拒请求 → 213 PAIRED (双路径重复下发) + 11 trail-covered + 3 TRUE EXPOSURE 短命微仓已结. 回归锁 20 测试 (death_of_defaults 8 + journal_firewall 12), 分块 ~5400 pytest 全绿. | FIX-20260819-002 |
| DQAF-20260816-001 | 2026-08-16 | Sev 3 | **pyproject 运行时依赖清单漏声明 lightgbm — CI 缺包阻断推送 (MANIFEST_OMISSION)**. CI `pip install -e ".[dev]"` 仅从 pyproject 装依赖, `[project].dependencies` 漏 4 个 Dockerfile 已声明的运行时 ML 依赖 (lightgbm/xgboost/scikit-learn/joblib) → 全新环境无 lightgbm → 8/3 FIX-20260803-007 新增 test_flow46_alignment.py::test_predict_is_yA_plus_r (L133 硬 import, 无 importorskip) 首次暴露; 此前 FIX-20260624-107 曾以 except ImportError 掩盖同根因于 meta_signal_filter.load(). 症状: CI shard-2 1 failed/600 passed exit=1, GitHub 推送未成功. BlastRadius: 纯 CI/构建管道, XAU/BTC 实盘零影响. | **CLOSED** — FIX-20260816-001 (IC Approved 2026-08-16): `[project] dependencies` 补 scikit-learn>=1.5,<2.0 / joblib>=1.4,<2.0 / lightgbm>=4.3,<5.0 / xgboost>=2.1,<3.0 (引脚对齐 Dockerfile:38-41); 验证 test_flow46_alignment 5 passed + pip dry-run 无冲突 (CI 将装 lightgbm 4.x) + verify --quick clean; L2 → L2 根治, 双清单收敛. | FIX-20260816-001 |
| DQAF-20260807-003 | 2026-08-07 | Sev 1 | **核心记账与终结管线四重断裂 (Ghost Volume / Stateless Gate / Dispatch Truth / Partial Fill)**. (1) 体积记账 ghost — reentry decay 后置覆写 decision.volume (0.02→0.01) 于 portfolio_risk snapshot 之后 → 派发 0.02 记账 0.01; 平仓 corpse 记 volume=0.0 (实证 4454299643: XAUUSDc m30_swing short 开 0.02 lot, PnL=−66.30 verified_from_mt5_deal, 被误判 close_without_open 隔离进 quarantine → SSOT 主账本缺失平仓腿 → DQAF-20260807-002 审计误读"仍持仓"). (2) _close_dispatched 耦合 PnL 观测 — 已物理派发的网络请求因算不出 PnL 被标记失败 → 派发(物理动作)与观测(PnL)不分离. (3) pending_close 盲等 50 分钟 — management 对 partial fill (MT5 残余>0) 无感知, 不逐周期重发 close → 半仓裸露 50 分钟. (4) 多进程 JournalGate 内存态漂移 — live_intent_loop/bridge/daily_ops 各实例 _known_tickets 不同步, 合法 open 在 gate 内存缺失 → 平仓误判孤儿. | **CLOSED** — FIX-20260807-003 (IC 雷霆裁决 4 令 + 体积单源化合并, Sev 1 外科手术): Boundary 1 **The Stateless Gate** — journal_gate.validate_close 每次调用 _reload() 重扫物理 journal (SSOT 在硬盘不在内存, 多进程零漂移, 无 IPC); 执行令 2a **The Dispatch Truth** — managed_close _close_dispatched=物理派发成功, PnL 观测剥离门控; 体积单源化 — position_registration 强制消费 DispatchResult.volume 为唯一记账真值 (decision.volume 仅退化回退); 执行令 2b **Partial Fill State Machine** — management_phase 新增 _probe_mt5_residual/_is_partial_fill (residual<expected) → 下一周期立即重发 close, _finalize_close_dispatch 保留残余 (pending lock 保持激活), sync_position_volume 只降 pos.volume 不降 full-close target; Step 3 **Zombie Resurrection** — 合法 reconciliation 回填 4454299643 PnL 尸体至 SSOT 主账本 (scripts/_reconcile_zombie_4454299643_20260807.py, 经 _append_journal+Stateless Gate, landed + 幂等 already_settled). 回归锁 17 测试 (2 Stateless Gate + 1 Dispatch Truth + 1 Watchdog + 13 Partial Fill) 全绿, validate_blueprints 0 STALE. ReB: STATEFUL_GATE_MULTIPROCESS_DRIFT + REGISTRATION_VS_DISPATCH_VOLUME_DIVERGENCE + DISPATCH_OBSERVATION_COUPLING + PARTIAL_FILL_BLINDNESS. | FIX-20260807-003 |
| DQAF-20260807-002 | 2026-08-07 | Sev 1 | **系统性高位接盘/低位追空 — 趋势追家族空间维度裸奔 (TREND_CHASE_NO_POSITION_GATE)**. 全历史 640 笔审计 (Iron Law #11): 多头平均 H1_z=+0.034; LONG H1_z>+1.5 桶 = 25.7% 胜率 / **−49.29 总盈亏 (全表最差桶)**; SHORT H1_z<−1.0 桶 = 44.3% 胜率 / **+99.36 (盈利桶)** — 非对称铁证. 趋势追家族 (m5/m15/m30/h1/h4/daily_swing) 全部守卫 4aa-4d 均为方向闸 (ADX/TF 一致/z 拐点), 零价格位置闸 → ML 动量特征在区间极值处峰化 → 结构上高位接多/低位追空. 根因 L3: check_z_inflection 被硬限制 statarb/ou (trend_isolation_gates.py:205). | **CLOSED** — FIX-20260807-002 (IC 雷霆裁决 Option A+C, 绝对禁止 Option B 大周期回撤进场 8/19 前): trend_isolation_gates.py 新增 4e Spatial Z-Score Gate (非对称): LONG H1_z>+1.5 (ranging 收紧 +1.0) → 硬否决 BLOCKED 绝不高位接盘; SHORT H1_z<−1.5 (ranging 收紧 −1.0) → 仅降额 volume×0.5 (低位卖出仍可行). 数据接入: 从运行时 40-dim v9 特征向量提取 H1_Price_ZScore (schema registry 动态索引, fail-open 缺失/非有限→放行). 调用点 strategy_line.py counter-trend 动作块之后 (避开 _ct_vol_mult 覆写陷阱). 回归锁 19 测试 (TestGate4eSpatialZScore 14 + TestExtractH1PriceZScore 5) + 36 gates + 43 strategy_line + 全量 5031 pytest PASS. 红线: 信号生成内核零触碰, 仅收紧风控底线. | FIX-20260807-002 |
| DQAF-20260807-001 | 2026-08-07 | Sev 1 | **XAU GodsEye fail-open 过度放行 + Zombie 无账逃逸 (双根因, IC 雷霆裁决)**. (1) GodsEye fail-open 教条 (strategy_evaluator Cut 7 "NEVER blocks outright") — health 0.52 首单 (08-06 09:44Z) 仍成交; 三单全 SHORT 追逐 "-$144 学费单", 低健康/choppy 期零硬风控底线. (2) Zombie 无账逃逸 (资金对账漏洞): pre_mgmt_zombie_cleared 的 settlement enqueue gate 在 `_z_open is not None` — bridge-direct journal 写持仓进 position_manager 但不进 known_open_tickets → `_z_open=None` 时仅 clear_position 无 settlement → 无 PnL corpse (实证 m30 ticket 4448694178: 11:20Z 开 / 13:09:55Z zombie / 09:47Z 平, settlement queue 恒空). 双刀: Option A Chop Filter + Zombie 原子补丁. 否决 B (P_win 非中性校验 — Cold Explore 学费继续), C (置信度阈值), D (结构位置过滤). ReB: TREND_CHASE_FAILOPEN_LOW_EDGE + ZOMBIE_ESCAPE_NO_CORPSE. | **CLOSED** — FIX-20260807-001 (IC 绝对批准): (1) Option A — strategy_evaluator Cut 7 新增 `_GODS_EYE_HARD_BLOCK_HEALTH=0.55` 硬否决: `_ge_health<0.55 OR _ge_chop` → `should_trade=False/volume=0.0` + BLOCKED_BY_GODSEYE 显式传递 + `gods_eye_blocked_entry` 事件日志, 既有 mode-gating 包入 else (0.55 边界不否决, 健康正常眼不否决). 08-06 09:44Z health=0.52 首单必被拦截. (2) Zombie 逃逸 — live_cycle 新增 `_zombie_settlement_fields` helper (z_open→position_manager 快照字段回退) + enqueue 条件改 `state.pending_settlement_tickets is not None` (无条件) — 任何退出 MT5 的持仓必留 PnL 尸体 (settlement poll / timeout 写 journal). 回归锁: TestGodsEyeHardVeto 5 测试 (production replay health=0.52/0.877 + chop + 0.55 边界 + 健康) + test_zombie_settlement 4 测试 (m30 4448694178 replay / 双缺安全 / enqueue 契约). 验证: tests/runtime 351 + live_cycle 直接导入者 40 + 回归 19 PASS; pre_commit_mypy 无新错误; ruff 4 文件 clean; verify --full exit 0. 红线重申: 8/19 前零重构, 仅收紧风控底线, 信号生成内核零触碰. | FIX-20260807-001 |
| DQAF-20260806-003 | 2026-08-06 | Sev 2 | **XAU 阈值共振卡死 — 健康态零开单 (8/04 静候假设被证伪)**. GodsEye health 0.697-0.875 恒 recommended_mode=normal, conf 最高 0.877 (08:05 h1_swing), 每周期 min_economic_volume_blocked 0.0102-0.0175<0.02 — 8 月实盘仅 8 笔 (3 天) 侥幸越阈. ECoL 铁证 (data/logs/intent_20260806T072045Z.log): 07:21 m30 0.0111 KILL / 08:05 h1 0.0175 KILL / 08:15 m30 0.0167 KILL; 08:05 health=0.875 cm=1.10 → **health×cm=0.963 ≫ 8/04 解锁线 0.70 仍全 KILL → fail-closed 静候论被实证推翻**. 双根因: (L3 config) live.yaml regime_map 缺口 — swing 家族仅 low_vol 出现 → regime_gate.py:815 默认 "reduced" (×0.65) 压低 raw target, normal/trending/mild_trend/ranging/high_vol 全缺席 (live_btc.yaml 对照完整); (L2 logic) strategy_evaluator.py:1108-1110 GodsEye health 乘数 max(0.25,health) 违反自身 "normal: no modification" 契约 (L1103), 且作用在 volume 已收敛到 0.02 min_economic floor 之后 → 健康态也 shave 跌破 floor. 附加收敛因子: cold_explore p_win=0.50 压低 kelly (E12, 非主因). AR 三反假设: 静候恢复 → E2b 推翻; cold explore 主因 → 部分存活 (贡献收敛非主因); Ω 终门诚实绞杀 → 部分存活 (单闸门设计正确, 但 health 乘数越权产生系统性误杀). BlastRadius: XAU 全 swing 家族系统性零开单 + 依赖成交的 calibrator/p_win 冷启动死锁 (鸡生蛋); BTC 零波及 (regime_map 完整, 防御态不变). ReB: THRESHOLD_RESONANCE_VOLUME_SHAVE. | **CLOSED** — FIX-20260806-007 (IC 终局裁决: Option A regime_map 补全 + Option B2 GodsEye 健康段饱和斜坡; 否决 B1/B3, Option C cold-explore 链 8/19 后): (A) configs/live.yaml regime_map 补全 6 个 swing 策略 ×5 regime 镜像 BTC (normal/trending/mild_trend=full, ranging/high_vol=reduced, low_vol 现状保留); (B2) 新增 `_gods_eye_health_vol_mult()` 死区斜坡替换 max(0.25,health) — health≥0.70→1.0 零干预 (死区), 0.25-0.70→连续线性斜坡 0.25x..1.0x (比例控制带), <0.25 钳位 0.25 (既有 floor 保留). 红线: min_economic 0.02 floor 不变 / 手数倍数不变 / GodsEye 置信·模式闸门不变 / BTC 防御态·文件不变. 回归锁 6 测试 (纯函数 deadband/ramp/floor-clamp + E2E decisive case 0.02×health0.875→PASS 存活终门 + degraded case 0.02×health0.30→仍 KILL). 403+ 套件验证 + verify --full 链 (蓝图登记后重跑). | FIX-20260806-007 |
| DQAF-20260805-003 | 2026-08-05 | Sev 4 | **trade_journal TRAIL_TELEMETRY_BLINDSPOT 探针口径过时 (Semantic Drift)** — 每轮误报. health_checks.py:101 精确键 `"trail" not in labels` 未随 FIX-20260612-003 label 契约演进 (reconciliation.py:193-217 写 sl_hit_trailed, 注释称 "closes the TRAIL_TELEMETRY_BLINDSPOT") → 精确键永失配. 实证: 全量 journal 2 条 sl_hit_trailed (均 2026-06-10), tail-500 窗口 (7/21-7/24) 0 条 trail label. AR: "遥测确实缺失" 反向假设被全量 2 条 sl_hit_trailed 部分推翻 — 契约对齐修正, 但窗口内真缺失. 修复: 包含性匹配 `not any("trail" in k for k in labels)` — 契约对齐正确 (回归锁 4 测试), 实库警告保留 (诚实信号). ReB: SEMANTIC_DRIFT_MONITOR_PROBE. | **CLOSED** — FIX-20260805-009 (IC 行动令 "Sev 4 Lite DQAF", 绝对批准选项 A): 探针契约对齐; 实库警告保留 (真缺失). 衍生 DQAF-20260806-001 (Sev 2 OPEN). | FIX-20260805-009 |
| DQAF-20260806-001 | 2026-08-06 | Sev 2 | **trail 出场遥测缺口根治 (逻辑层失忆, 物理层健康)** — IC 终局调查令 ECoL 取证: 近窗 (7/15+) 81 笔 sl_hit_first 中 **41 笔 SL 物理移动过** (40 笔 ≥1.0R trail 级收紧, 23 笔 ≥3 级, 最高 85.6R), **MT5 成交价反证 40/41 落移动后 SL** (broker 真实执行 Chandelier trail) → 物理层健康, 逻辑层失忆. 根因 (L3 CONFIRMED): `position_close_adapter.py:439-440` 对 DEAL_REASON_SL 硬编码 sl_hit_first (Strangler Fig #11 FIX-20260611-005 换生产方时 trail-aware label 契约未迁移; FIX-20260612-003 打标落在重启-only reconciliation 路径 live_cycle.py:1503) → 6/11 后 journal 0 条 sl_hit_trailed. AR 否决竞态假设 (adapter 无 state 参数, 结构上读不到 trail_advances). BlastRadius: BTC 近窗 49% (40/81) SL 出场实为 trail 出场被误标 → 出场归因/p_win 校准/策略评估全污染; XAU 同构 (adapter 共享). ReB: LABEL_PRODUCER_SWAP_SILENT_AMNESIA. | **CLOSED** — FIX-20260806-001 (IC 绝对批准 Option A 外科手术): `state.position_manager` trail_advances 注入 `_build_event`/`detect_and_build` (镜像 reconciliation.py:198-204) + MIA trail_contribution fallback (镜像 mia_close.py:180-185, 恢复 Strangler Fig #12 丢失语义); 回归锁 6 测试; 403 runtime tests PASS + verify --quick PASS. **Option C (三路 label 单源统一) 记 Deferred 架构债** (8/19 后清偿). 法证探针 scripts/_audit_trail_mislabel_20260806.py 留工作树. | FIX-20260806-001 |
| DQAF-20260806-002 | 2026-08-06 | Sev 3 | **测试幽灵告警泄漏实盘钉钉群 (Test→Prod Alert Leak)** — 05:03:23 三条 `phantom:test_fail/test_neg/counter_test` CRITICAL 推送, 审计日志同毫秒铁证 (alert_audit.jsonl 2026-08-06T05:03:23.270512/.285516/.397541). 根因 (L2 CONFIRMED): `core/contracts/phantom_contract.py:899-933` `_alert_violation()` 对 `@phantom` 违例硬编码 `LiveAlertHub(base_dir="data")` → `send_critical(reason=f"phantom:{id}")` → env `QUANTOS_DINGTALK_WEBHOOK_URL` (机器级已设) 接线真实钉钉 → 测试故意触发违例验证机制时**真实推送**. contract_id 与 tests/contracts/test_phantom_contract.py 逐字对应 (L177-207/850-857), 生产 predicate id 永不匹配 (risk_budget_non_negative 等). 触发时机: 05:03:23 = A3 验证期 pytest 运行 (11:53/11:55 亦同, 此前 FIX-20260805-006 关键词修复前被 errcode=310000 静默吞掉). AR: "实盘违例?" 被生产 id 对照驳回. BlastRadius: XAU/BTC 零交易影响; 风险=CRITICAL 告警信号退化 (狼来了) + 每次违例新建 hub (后台 worker 线程泄漏 + 写实盘 data/logs). ReB: TEST_TO_PROD_ALERT_LEAK. | **CLOSED** — FIX-20260806-005 (IC 绝对批准 Option C, 否决 Option A "测试诱导设计破坏"): 测试侧物理缴械零生产改动 — test_phantom_contract.py autouse fixture 让 `LiveAlertHub.__init__` raise ImportError → `_alert_violation` 走既有 stderr fallback (计数逻辑保留) + tests/conftest.py 全局 autouse `delenv` 四 webhook env 物理致盲 (兜底). 66+40+300 tests PASS, 审计日志零增长 (15082 不变), ruff + pre_commit_mypy clean. | FIX-20260806-005 |
| DQAF-20260805-002 | 2026-08-05 | Sev 2 | **journal_completeness 假 CRITICAL 每次重启 — FIX-20260611-005 临时补丁超期 25 天未退役。** 重启后 ~7s 新进程即发 JOURNAL_SLA_VIOLATION: dupes=123>10 是唯一 FAIL flag (close_price=100%/trail=114.5% 全达标)。123 dupes 全为 Phase 2 retry-reentrant 重写残留 (独立探针 _audit_dupe_category: 123/123 Cat A 同 pid 同 deal, Cat B=0, Cat C=0, 8/1 后零新增)。根因 (L2): FIX-20260611-005 自声明"Phase 2 落地后转 structural guarantees"却从未退役 — `_expiry='2026-07-11'` 纯消息字符串零退役逻辑, 检查仍按原始投影行数键 (ticket, ack) 判 dup, 未消费已落地的幂等事件身份 (deal_id)。技术债 TODO-20260711-journal-idempotency 超期 25 天。BlastRadius: BTC 每次重启假 CRITICAL 钉钉噪音 + 告警疲劳 (8/19 脱敏风险), 零资金/数据风险 (全路径按 ticket 去重); XAU 同机制 (dupes=4)。ReB: EXPIRED_TEMP_GATE_UNRETIRED。 | **CLOSED** — FIX-20260805-008 (IC 批准 "The Idempotent Key"): 移除 _expiry/[EXPIRES] 框架; dedup 升级为 Phase 2 幂等事件身份 (position_identifier, deal_id) — retry residue metrics-only 不 FAIL (实库 212 残留, 独立探针复核一致), 同仓 ≥2 不同非零 deal_id → ambiguous_events FAIL (实测 0), deal_id=0 不参与歧义; 保留 close_price/trail FAIL; 主码 JOURNAL_SLA_VIOLATION + duplicates 键向后兼容; 页脚去重 (Boy Scout); 实库实证 run_data_health.py: journal_completeness JOURNAL_SLA_OK; 回归锁 test_journal_completeness_phase2.py 10 测试。 | FIX-20260805-008 |
| DQAF-20260805-001 | 2026-08-05 | Sev 2 | **hash-lock 门禁 stat 幽灵 (Stat Phantom) — 8/19 决战 gate 假阳性风险。** 启动 reconcile 经 `_save_live_yaml` (brain_lifecycle_manager.py:205-211) 无条件重写 live_btc.yaml — 内容与 index 逐字节相同 (blob 76f78fcd, CR=0) 仅 mtime 刷新 → `git status --porcelain` 持久伪 ` M` (6×1s 采样全中) + `git diff` 空 + `update-index --refresh` 失效, 仅 `git add`(no-op) 清除。两 hash-lock gate (`train.py:1311` `_enforce_hash_lock` + `daily_flow46_precheck.py:152`, L71 "never drift" 契约) 均 stat 基 → 8/19 `_enforce_hash_lock` 假阳性 SystemExit 风险。CCT 3 层链 (症状→reconcile 无条件重写等价内容→stat 基 gate)。BlastRadius: BTC 8/19 决战 + 每日预检 Sev1 噪音; XAU 同机制。ReB: HASHLOCK_STAT_PHANTOM。 | **CLOSED** — FIX-20260805-007 (IC 绝对批准 "The Content-Based Gate"): 三处 hash-lock 全改内容基 `git diff HEAD --name-only` (幽灵+CRLF 伪差异免疫) + `git ls-files --others --exclude-standard` (untracked source 阻断, `_audit_*.py` 法证探针豁免); canonical 加 cwd 测试参数。回归锁 test_hash_lock_content_gate.py 7 测试 (幽灵免疫/真实变更阻断/探针豁免/新增文件阻断/allow-dirty/gitignore)。实库验证: 幽灵场景 `git diff HEAD` 空 + 伪差异归零。 | FIX-20260805-007 |
| DQAF-20260804-005 | 2026-08-04 | Sev 3 | **BTC_Flow46_V1_SHORT 脑"死物" — runtime base+residual 求值器未接线 (T30② / IC 最高行动令)。** 46-dim freeze-and-residual 脑 (OOS ρ=0.0721) shadow 注册后永不投票: BrainFactory 按 brain_type→BRAIN_TYPE_MAP→LightGBMBrainAdapter→load() 仅加载 5-feature 残差 → `_num_features=5` → `_run_inference` 组装 46-dim → infer() 维度守卫 fallback 0.0 (静默中性); 且 config 缺 `training_params.objective` → validator 动态推断 (brain_type 不 startswith lightgbm) 失败 → ghost-brain ERROR → BrainConfigError 加载即拒。CCT 3 层链 (症状→维度守卫+validator 双断链→训练侧 `ResidualTransferLearner.predict` 组合逻辑零运行时调用者, L3 架构不完整 RC-06)。BlastRadius: shadow 脑零资金风险 (vote_weight=0.0), XAU 零波及, LONG 塔 8/19 同根 (同 transfer 元数据自动继承)。ReB: CONTRACT_WITHOUT_RUNTIME_EVALUATOR。 | **CLOSED** — FIX-20260804-005 (fe64d577, IC 最高行动令 "Proceed to Execution"): 新建 `TransferResidualBrainAdapter` (继承 LightGBMBrainAdapter, load 双 booster + infer 组合 y=y_A(X[:, :base_n])+r(X[:, base_n:]), 与训练侧 bit-identical, base/residual 分解诊断) + BrainFactory `transfer.kind==freeze_and_residual` 元数据分派 (方案 A, brain_type 保留 expected_r_short 信号语义) + brain_config.py 一等参数 training_params/transfer + Flow46 config +3 字段 (objective/activation_threshold/frozen_base_artifact_path)。8 单测全绿 (mock+真实产物双路 bit-identical / BrainFactory 复活 Flow46 / V5 塔回归锁 / fail-closed) + 434 related PASS + mypy+ruff+blueprint clean。 | FIX-20260804-005 |
| DQAF-20260804-003 | 2026-08-04 | Sev 2 | **XAUUSDc_return slot [12] 恒零 — 跨资产特征架构不一致 (重启后验证发现)。** AUDJPYc_return [30] + BTC/XAU ratio [39-40] 走 MT5 直读已恢复真实数据 (FIX-20260804-002), 唯独 XAUUSDc_return [12] 走特征仓跨符号读 — XAU 跨仓供给稀疏 (4-6h/条人工 feature-update, 53 条, 最后 08-04T03:35Z), 与 5 分钟 staleness guard 完全不匹配 → slot [12] 恒零 (193/193)。DA 4 源独立证据: 特征仓 193 条 _v2 记录 slot 审计 (XAU=0 / AUDJPY+Ratio 真实) + XAU 跨仓 53 条时间戳分布 (4-6h 节奏) + 代码路径对比 (三特征两数据源) + 实盘日志 (staleness guard 正确触发); AR: "XAU 数据不可用" 反向假设被 53 条真实 M5_Ret_1 记录推翻; CCT 3 层链 (症状→guard 恒触发→稀疏 feed 与 guard 不匹配→架构不一致); BlastRadius: BTC slot [12] 特征恒零 (模型少一个跨资产信号), XAU 无波及。ReB: SPORADIC_FEED_VS_MT5_SSOT。 | **CLOSED** — FIX-20260804-003 (IC 终局裁决, 方案 a 批准/方案 b 否决): `_compute_xauusdc_return` 改 MT5 直读 (`copy_rates_from_pos("XAUUSDc",5)` + `_bar_close`), 三特征统一 MT5 SSOT; 删除特征仓读路径全部死代码 (构造参数/助手/常量/状态); live_cycle 3 构造点清参; 测试重构 worker 单一来源。47+215+82 tests PASS; mypy+ruff+verify --quick clean。 | FIX-20260804-003 |
| DQAF-20260801-008 | 2026-08-01 | Sev 3 | **启动日志 8 类告警 (重启 V4.2 双塔 shadow 后)。** (1) 3 brain 缺 artifact_hash; (2) SSOT drift M15(cfg=retired/gov=probation)+V4_LGB(cfg=candidate/gov=probation); (3) exit_config_validation_warning ev_trajectory_enabled 未知 key ×5 策略线 — validator 白名单滞后运行时消费 key (management_phase.py:1853 实际读取); (4) H1_V2 artifact_hash mismatch (文件替换 config 未同步) + V12_Survival 16 字符截断 hash; (5) V4_LGB missing_yaml_entries (informational, auto-discovery); (6) Freshness Contract 3 VIOLATION — telemetry 产物 (EXECUTION_STATE/MT5_BRIDGE_HEALTH/ALERT_COOLING) TTL 套用 batch max_age 6h 属分类学错误; (7) conformal_ou_gate disabled (设计行为); (8) 7 python 进程 (无双实例, 3 stale pytest 残留)。DA: 6 源独立证据 (启动日志全读/5 brain config/governance_state/catalog.py:508/strategy_config_validator.py:14/management_phase.py:1853); AR: 双实例假设被 wmic 进程链推翻; CCT 2 层链 (症状→validator 白名单滞后 / 单 producer max_age 契约误套 telemetry)。BlastRadius: BTC 告警噪音零资金风险, XAU 无波及 (live.yaml kalman key 同根)。ReB: WHITELIST_LAGGING_RUNTIME_KEYS, STRANGLER_FIG_CALLER_NOT_MIGRATED, TELEMETRY_TTL_VS_BATCH_CONTRACT。 | **CLOSED** — FIX-20260801-008: (A) 5 brain config artifact_hash 对齐 sha256; (B) M15 governance demote→retired + V4_LGB config→probation; (C) _EXPECTED_EXIT_KEYS 补 4 key + live_cycle 重复副本消除 (canonical import); (F) StateArtifact producer_class telemetry 隔离; (H) 3 pytest 僵尸进程清理。startup_integrity hardcoded_path_mismatches 4→0, exit-config warnings 5→0, freshness violations 3→0。 | FIX-20260801-008 | **V4.2 Expected R 双塔完全盲区 — feature_dimension_mismatch expected=37 got=40 每周期, btc_expected_r_m15 零信号(9 条 golden_master 全 neutral/零投票)。** btc_expected_r_37 schema 在 registry (FIX-20260731-004) + FeatureService 白名单 (FIX-20260801-001) 注册后漏了运行时分发管线 6 处: feature_assembler 路由条件、feature_router SCHEMA_CONTRACTS、build_lake 子集 zip (29/37 位置偏移)、live_cycle/management_phase 路由+btc_augment gating、swing_strategy _needs_daily+btc_augment。DA 3 源独立 (intent brain_alert / feature_assembler:92-97 / 复现脚本); AR 4 反向假设全推翻; CCT 3 层链 (症状→路由遗漏 fallback 40-dim→新增 schema 未同步分发点); BlastRadius BTC 双塔 shadow 无资金风险, XAU 无波及, flow_46/h1_48 同根潜在。ReB: SCHEMA_ROUTING_MISSING_NEW_SCHEMA。 | **CLOSED** — FIX-20260801-007 (30eee7ad): 6 处分发点全部同步 + build_lake 改 canonical 41-dim names zip (子集 schema 按名提取, 位置无关) + btc_expected_r_37 fail-closed 分支。17-assert 路由验证 misaligned 0/41 + 双塔端到端真实推理, legacy shim/v2 bit-identical。 | FIX-20260801-007 |
| DQAF-20260726-012 | 2026-07-26 | Sev 3 | **Ghost-position bootstrap gap — journal bootstrap 与 execution_state restore 互斥而非互补。** `live_cycle.py:1447` 的 `not state.known_open_tickets` 条件使 restore 填充任何仓位后 bootstrap 被完全跳过 → 在 downtime 中平仓的幽灵仓位永不播种 → reconciliation 永不检查 → 跨重启永久存活。ReB: GHOST_BOOTSTRAP_RESTORE_MUTUAL_EXCLUSION。 | **CLOSED** — FIX-20260726-012: 移除 `not state.known_open_tickets` 守卫。Bootstrap 始终在 `loop_iteration==1` 运行并与 restored state ADDITIVE 合并（仅添加新票号）。通过 message_id 匹配实现幂等（非可变 position_ticket）。 | FIX-20260726-012 |
| DQAF-20260724-001 | 2026-07-24 | Sev 1 | **M5_Vol_ZScore 过去 2+ 天几乎全日为负（最大值仅 -1.34），导致 vol_zscore_hard_block 持续熔断所有 swing/trend 策略。** 3 源证据锚定（feature_store 39,714 条 94% 非正 / golden_master 全天 blockade / `_vol_zscore` 全链路审计确认 inclusive-window 算法无误但 CFD tick_volume burst-decay 分布致结构性偏差）。AR 双轨验证（price_zscore 49/51 pos/neg 证明算法正确 / CSV 仿真 39.2% 正 → CFD 实时数据特有）。CCT 二层链（症状：vol_zscore_hard_block ×N → 中间：M5 tick_volume burst-decay → 根因：电路断路器锚定于合成 CFD 伪指标）。投委会裁定：否决阈值妥协（方案 A），批准独占窗口（方案 C，但冻结至重训周期），强制提权 P2→P0（方案 P2：ATR 比率法）。ReB: CIRCUIT_BREAKER_ANCHORED_TO_SYNTHETIC_CFD_PSEUDO_METRIC。 | **CLOSED** — FIX-20260724-001: 摘除 Vol_ZScore < -3.0 硬闸门，替换为 ATR 比率熔断 `atr_ratio = current_atr / mean(buffer_sample_50)` 阈值 0.5。`_load_atr_buffer_sample()` 从 regime_detector_state.json 加载 buffer_sample，60s 缓存 TTL，全错误路径 fail-open。阈值 0.5 为临时热修复，已登记 Deferred 历史回测校准任务。 | FIX-20260724-001 |
| DQAF-20260718-001 | 2026-07-18 | Sev 2 | **Orphan 合成 close 条目随 compact_journal() 父 open 被剪枝后残留在 journal 中。** `cleanup_orphan_opens()` 写入 `label="auto_orphan_*"` synthetic close，但 `compact_journal()` 单条目压缩无级联删除逻辑 → 孤儿 close 与已剪枝的父 open 永久残留。ReB: ORPHAN_CASCADE_DELETE_MISSING。 | **CLOSED** — FIX-20260718-001: compact_journal() two-pass cascade: Pass 1 收集被剪枝的 open message_id → Pass 2 删除匹配的 auto_orphan_* synthetic close (by open_message_id)。返回 cascade_removed 计数。 | FIX-20260718-001 |
| DQAF-20260718-002 | 2026-07-18 | Sev 2 | **XAU daily_ops 定时对账将 BTC 治理排除在外 — cmd_reconcile() 路径硬编码。** brain.py reconcile 子命令所有路径 (brains_dir, gov_path, live_path, ledger_path, perf_path) 硬编码为 data/brains (XAU) → 定时 daily_ops 调用的 SSOT 对账对 BTC 无效。BTC governance_state 漂移未被 cron 检测到。ReB: SINGLE_ASSET_HARDCODED_PATHS。 | **CLOSED** — FIX-20260718-002: cmd_reconcile() 参数化 (--data-dir, --brains-subdir, --live-config); daily_ops.py 从 base_dir 契约派生双资产路径 (data→brains, data_btc→brains_btc)。 | FIX-20260718-002 |
| DQAF-20260718-003 | 2026-07-18 | Sev 3 | **ConformalOUGate 静默 passthrough — statarb 策略的 OU 质量零可观测性。** XAU OU brain 配置已归档 (FIX-20260625-136，统计显著亏损) → ConformalOUGate 找不到配置 → 静默 bypass（零日志）。无法检测或衡量未受门禁约束的 statarb 信号质量。ReB: SILENT_GATE_BYPASS_ZERO_OBSERVABILITY。 | **CLOSED** — FIX-20260718-003: passthrough 路径接入节流 WARNING (每 100 周期) + `_passthrough_count` 按原因追踪 + `describe()` 暴露诊断数据供监控集成。 | FIX-20260718-003 |
| DQAF-20260710-001 | 2026-07-10 | Sev 2 | **TP trail profitability gate missing — compute_trail_tp() 纯波动率函数零盈亏感知。** XAU ticket 4108944294 (SHORT@4107.27) 从未盈利, 但 ATR 收缩触发 25 次 TP 收紧 (3866→4060)。SL trail (compute_trail_stop) 有 trail_activation_atr 盈亏水位线 (FIX-20260603-064), TP trail 无对等保护。ReB: TP_TRAIL_NO_PROFITABILITY_GATE。 | **CLOSED** — FIX-20260710-002: highest_high<=entry_price (LONG) / lowest_low>=entry_price (SHORT) 盈亏前置闸门置于 ATR 收缩检查之前, 镜像 SL trail 已验证模式。9 tests (3 new)。 | FIX-20260710-002 |
| DQAF-20260709-003 | 2026-07-09 | Sev 2 | **XAU 在持单止盈逻辑坍缩 — per-TF ATR 半迁移。** compute_trail_tp 的动态收紧 TP 用 M5 尺度 current_atr 计 `tp_distance=mult×current_atr×1.75`, 而开仓 SL/TP 由 per-TF(H4) ATR 定尺 (FIX-20260706-027 via dynamic_sl_tp)。ATR 一收缩 (atr_ratio≤0.80) 即用 M5 小尺度 TP 覆盖 H4 大尺度开仓 TP: live h4_swing 4103318355 TP 从 3823.46 (RR 1.66) 被逐周期拉到 4044.8 (距 entry 11 点, RR 0.08, 冒 140 博 11)。影响半径 (Iron Law #11 脚本): h4_swing 162/436(37.2%) + h1_swing 80/625(12.8%) 快照 RR<0.5。根因 (L3): FIX-20260706-027 把 per-TF ATR 注入**开仓定尺**但未同步到 pos.entry_atr(存 M5) 与管理期 current_atr(M5) → bracket-relative 消费者在错误尺度运算。AR 推翻三反假设 (SL/TP 开仓即异尺 / H4 真收缩 / entry_atr 是定尺 ATR)。 | **CLOSED** — FIX-20260709-004 (0531a00d): 新持久化原语 ActivePosition.bracket_atr = 定尺 per-TF ATR (注册时从 tf_atr_map 取), compute_trail_tp 距离乘 bracket_atr/entry_atr → TP 留在 bracket 时间框。收缩 GATE 不变 (尺度不变量)。entry_atr **刻意不动** (R 度量/ratchet FIX-20260708-004/MetaExit atr_entry 特征的 M5 参照, 改之须 backtest → Deferred)。bracket_atr=0 精确复现旧行为。+#10 fail_open_guard 硬化注册。6 回归测试。几何余项 (Chandelier/breakeven/proximity/R 度量) **已收口** → FIX-20260709-006 (geometry remainder: breakeven + Chandelier + graduated_lock + max_lock 全部换 bracket_atr, 阈值折叠 3.0→1.0/5.0→2.0, max_lock 4.0→2.0, ratchet 刻意不动 entry_atr)。backtest: 96.1% XAU / 94.3% BTC breakeven death survive。ReB: R_UNIT_MISMATCH_CROSS_TIMEFRAME (PER_TF_ATR_HALF_MIGRATION)。 | FIX-20260709-004, FIX-20260709-006 |
| DQAF-20260709-005 | 2026-07-09 | Sev 4 (IC revised from Sev 1 via AR) | **ExitWatchdog 死 evaluator (FIX-20260613-086) — evaluate_position 全库零调用者, 读取从未设置的 position.unrealized_pnl_r, time_decay/price_decay 恒不触发。** 原疑为 Sev 1 "静默安全网失效", AR 四源反驳: (1) unrealized_pnl_r 无注入, (2) evaluate_position 零调用者=死代码, (3) journal 零 time_decay_/price_decay_ 出场, (4) 职责已被 PositionManager.should_exit_hesitation (per-TF 缩放) 接管。非"失效的网",是"孤儿代码遗骸"。ReB: SUPERSEDED_ORPHAN_CODE_WITH_STALE_DOCSTRING。 | **CLOSED** — FIX-20260709-005 (446ba31f): 删除 3 方法 + BLE001 noqa 标化; 构造参数保留 vestigial (hot-path omega 约束, 待下次 live_intent_loop.py 变更清理)。零僵尸测试, watchdog 60 测试绿。 | FIX-20260709-005 |
| DQAF-20260709-002 | 2026-07-09 | Sev 2 | **XAU 两腿对冲且持仓不平 — 三相根因 (进场/持仓/出场)。** 出场 (L3): management_phase Guard 1 以 known_open_tickets 缺席推断"已平仓"直接 clear, **不查 broker**; 21:00 休市重启风暴 (13 次重启) 使 LONG 4098917446 脱离 known_open_tickets → Guard 1 `position_manager_stale_cleared` ×11 ↔ orphan 采纳 (仅 loop_iteration==1) → 乒乓脱管, LONG 开在 broker 却失管不平 (active_position_tickets=[SHORT] 缺 LONG, mt5_tickets=[both])。进场 (L3): CrossStrategyCoordinator (block 默认) 自 P4-2 从未注入 live (param 恒 None) → 反向持仓守卫死代码 → LONG 得以对冲既有 SHORT。持仓 (**AR 推翻**): SHORT SL=4162.674=2.0×H4_ATR(63.9) 正确定尺; "-6.5R"为 snapshot 用 M5 entry_atr(6.41) 算 R 的**单位错配度量假象** (实为 -0.65×H4_ATR ~26% 到 SL), 正常 H4 swing, **无交易缺陷**。取证 scripts/forensic_xau_hedge_20260709.py (Iron Law #11)。 | **CLOSED** — 出场 FIX-20260709-002 (broker 权威 stale-guard, 017d726d) + 进场 FIX-20260709-003 (CrossStrategyCoordinator 接线, 10e22cb2)。持仓相经 IC 裁决**搁置投机交易改动**, 两次生问题 (R 度量 ATR 错配 + bars_held 重启冻结) 登记 Deferred。ReB: BROKER_STATE_NOT_CONSULTED_BEFORE_UNTRACK + DORMANT_SAFETY_GUARD_NEVER_WIRED + R_UNIT_MISMATCH_CROSS_TIMEFRAME。 | FIX-20260709-002, FIX-20260709-003 |
| DQAF-20260709-001 | 2026-07-09 | Sev 3 | **analyze_live_journal.py 崩溃 — `.get("label","?")` 对 present-but-null 失效: `dict.get(k,default)` 仅 key 缺失时替换 default, key 存在值为 None 时返回 None → 114 条 XAU close 合法 `label:null` (no-ticket 孤儿分支遗留) 使 None 成 `pnl_by_label` 字典键 → :559 `{lbl:<55s}` format(None) TypeError, XAU 完整审计不可得。** 同构潜伏 :133 side/:136 ack。打印处补丁 FIX-20260613-066 (Section 4 trade_side/n_brains) 同类复发 → 证明须摄入边界根治 (Iron Law #12)。ReB: GET_DEFAULT_NULL_TRAP。 | **CLOSED** — FIX-20260709-001: `_coalesce(mapping,key,default)` helper (get 后 None→default) 在 trade_data 摄入边界单点规整 side/label/ack, None 永不达下游键/formatter; label 默认 `(unlabeled)` 诚实暴露 114 条 (34W/20L/+$0.68) 而非隐藏。两品种 EXIT 0 (XAU 恢复完整 7 段), 6 回归测试。BTC 0 null-label 不受影响。 | FIX-20260709-001 |
| DQAF-20260708-004 | 2026-07-08 | Sev 2 | **give-back (TP 到甚至超过却不平, 回撤打 SL) 执行侧真实资金损失** — Chandelier trailing SL 从未把正底线送达券商: 候选没推进就返 None (无底线), 候选用 current_atr 波动放大移动 goalpost, graduated_lock 首档 +3R 留 +1~3R 死区, breakeven 楼层依赖 breakeven_triggered **意图锁** (trail_dispatch:117 无论 feasibility-skip/reject 都置 True)。生命周期重建 (scripts/_diagnose_giveback_lifecycle.py, Iron Law #11): cohort BTC 121/XAU 74, MODE_B trail_never_locked 87-89% (主因)+MODE_C model_exit_at_be 80-84%, MODE_D bracket 反转仅 2.5-4% (**推翻原假设**, AR clause)。主拒绝码 10025 NO_CHANGES。样本 4090084166 (+1.44R 0改单)。ReB: PROFIT_RATCHET_NEVER_REACHES_BROKER。 | **CLOSED** — FIX-20260708-004: Profit Ratchet Floor (trail_stop_engine.compute_trail_stop) — peak r_max(entry_atr 稳定标尺)≥arm_r 强制 SL 锁 ≥max(0.1R, r_max−1.0R), 经 max()/min() 折入候选即使 Chandelier 返 None 也托底, 独立于意图锁, 单调抑 NO_CHANGES。broker-bound 楼层物理封顶回撤 → R1 结构性吸收 R2 (不动 V6 shadow)。+ratchet_floor_r 诊断。意图锁 Deferred。 | FIX-20260708-004 |
| DQAF-20260708-003 | 2026-07-08 | Sev 1 | **close_price 伪造 breakeven 根因 — position_close_adapter._build_event 盲取 deals[0]=DEAL_ENTRY_IN 入场 deal (adapter 每周期重建→cursor 恒 0) → close_price==entry_price, pnl==0, label==breakeven 于每次全平。** 三处 deal 选择实现分叉 (adapter 错, reconciliation/mia 对), 上游从未强制"close 取出场 deal"不变量 (同类 FIX-20260601-046 盲取 closes[0])。伪造账目污染 governance WR/PF + calibrator + 隐藏真实亏损 (用户实盘: TP 到却不平, 回撤打 SL)。地面真相 ticket 3947528377 (entry=close=63514.66, 次单 @64598.99)。ReB: BLIND_DEAL_INDEX_FABRICATES_BREAKEVEN。 | **CLOSED** — FIX-20260708-003: core/runtime/deal_selection.py resolve_exit_deal() SSOT 强制 close 取 DEAL_ENTRY_OUT; adapter/reconciliation/mia 统一 (消 3 分叉); provenance close_price_source/pnl_status; broker deal.profit 优先; backfill_fabricated_breakeven.py 检测 BTC 14 + XAU 1 (append-only, 同 SSOT)。写入咽喉统一 (PnlGuard→_append_journal, D1) Deferred。 | FIX-20260708-003 |
| DQAF-20260708-001 | 2026-07-08 | Sev 2 | **孤儿平仓/双轨标签频发根因 — open<->close join 键用可变 position_ticket 而非不可变 position_identifier。** MT5 partial-close/netting 换号 → 换号平仓被 JournalGate 当 close_without_open 隔离 (~17/day) + label_builder 静默丢弃 → 训练标签损失。34/35 identifier-matched-to-open 证明是键缺陷非写丢失。ReB: MUTABLE_TICKET_JOIN_ON_IMMUTABLE_POSITION。 | **CLOSED** — FIX-20260708-001: resolve_identity() 单一不可变身份解析权威; JournalGate/auditor/reconciliation/label_builder 改 identity-keyed + PositionOpened 补锚。BTC 孤儿 157→119, $126 回收。TECH_DEBT-003 SSOT key 更正 (ticket→identifier)。journal_gate freeze bypass (IC-approved)。 | FIX-20260708-001 |
| DQAF-20260707-003 | 2026-07-07 | Sev 2 | **BTC live H1 模型近 100% 输出 LONG — 方向偏置。** 41-dim btc_macro schema 缺 H1 时间尺度方向信号 (Wasserstein=0.0084)。补 7 个 H1 动量特征做 48-dim 重训 → cv val_wr xgb 50.8%/lgbm 49.0% = 掷硬币, Wasserstein 恶化至 0.0019 → H1 方向结构性不可分, 加特征救不了。ReB: FEATURE_ENGINEERING_CANNOT_RESCUE_UNSEPARABLE_SIGNAL。 | **CLOSED (退役而非部署)** — FIX-20260708-002: 退役 V12_H1_15 + btc_swing_h1 策略线; V4 采用 quantile_gaussian 校准 (已 live 于 8 XAU brain, T22 监控); golden_master 补 brain 级遥测; 48-dim serving 接线休眠留待 Path C horizon=4。 | FIX-20260708-002 |
| DQAF-20260706-003 | 2026-07-06 | Sev 1 | **Shadow brains (vote_weight=0.0) executing live XAU trades via Cut 4 governance degradation gate.** After Tracer Bullet Phase 2 deployment, restart initialization race caused only shadow brains to vote → `_live_count=0` → degraded path allowed shadows to drive 3 real-money decisions (H4 SHORT, M15 LONG, M30 LONG). Root cause: Cut 4/Cut 4-bis checked `status in (live,probation)` but NEVER checked `vote_weight>0` — 4th instance of cross-file duplicate gate logic bug in strategy_evaluator.py. | **CLOSED** — FIX-20260706-003: (A) Cut 4: `_has_voting_power` fail-closed gate blocks trade when all voters have vote_weight=0; (B) Cut 4-bis: `_voting_voters` excludes shadows from denominator (1 live+8 shadows→ratio=1.0 not 0.11). All three governance gate dimensions now covered: governance access (FIX-174), status filter (FIX-061), vote_weight (FIX-003). | FIX-20260706-003 |
| DQAF-20260628-063 | 2026-06-28 | Sev 1 | **BTC Ping-Pong 降级: V4 唯一 Live 大脑被 Governance Cycle 循环降级至 Probation → 0 Live → 全部策略停摆。** PnL Ledger 无 GC → 12 幽灵大脑每周期重新注册 → Quality Engine V4 评分 27.69→degraded + legacy WR=35.5%<45%→probation + RR-adjusted PF=1.15<1.3→blocked。Last-live guard (FIX-162) 放错代码路径被绕过。V4 3 小时内被降级 3 次, 每次 SSOT reconciliation 修复后下一周期再次降级。ReB: PING_PONG_DEMOTE。 | **CLOSED** — FIX-20260628-168: (1) Ghost filter: config SSOT 白名单拦截 PnL ledger 幽灵注册, (2) Last-live guard: transition() 调用点拦截 (填补 FIX-162 绕过缺口), (3) PF_RR_ADJUSTED_MIN 1.3→1.1 (V4 PF=1.15 救济)。 | FIX-20260628-168 |
| DQAF-20260630-202 | 2026-06-30 | Sev 2 | **H4_V3 Shadow Signal Desert — Macro brain 100% directional (456 SHORT) 但 stuck at candidate。** 双根因: (Contract 1) activation_threshold=0.1 全局 hardcode 使 H4_V3 raw_scores [-0.07, -0.12] 落入 dead-zone; (Contract 2) Rule 85 多样性检查 (long≥5 AND short≥5) 未豁免 H4/D1 macro timeframes — 真正的趋势跟随者应产生方向垄断。Rule 85 逻辑在 governance_rule_engine.py 和 governance_scheduler.py 重复实现, 初始修复仅覆盖前者。ReB: TOXIC_DIVERSITY_GATE, DUPLICATE_RULE_UNSYNC。 | **CLOSED** — FIX-20260630-202: (1) activation_threshold SSOT per-brain config, H4_V3→0.05; (2) Macro exemption in governance_rule_engine.py; FIX-20260701-203: BrainEntry.timeframe→raw.get() AttributeError 修复; FIX-20260701-204: duplicate Rule 85 fix in governance_scheduler.py + base_dir pass-through。 | FIX-20260630-202, FIX-20260701-203, FIX-20260701-204 |
| DQAF-20260628-062 | 2026-06-28 | Sev 2 | **Config→Governance 双轨漂移: 大脑配置变更后治理状态未同步。** 49 大脑 config 注册但 governance 仅 18 条目 — 31 大脑指标注入静默失败。ReB: CONFIG_GOVERNANCE_DUAL_TRACK_DRIFT。 | **CLOSED** — FIX-20260628-062: `_step_config_gov_reconcile()` 对齐管道 — 读取 brain_registry_entry.v1 configs, 自动注册缺失大脑 (candidate), 记录漂移不覆写生命周期 (FIX-20260613-076 契约)。 | FIX-20260628-062 |
| DQAF-20260628-061 | 2026-06-28 | Sev 2 | **XAU 治理盲眼: 49 大脑有 PnLStore 数据 (144K settled) 但仅 18 个接收治理指标注入。** `set_performance_metrics()` 对未注册大脑静默跳过, scheduler_service purge 清空 daily_ops 注入指标。ReB: GOVERNANCE_REGISTRATION_SILENT_SKIP。 | **CLOSED** — FIX-20260628-061: (1) governance_scheduler auto-register 未注册大脑, (2) _data_source "no_data" 标记, (3) scheduler_service purge 感知 _data_source 字段。验证: governance 18→54 brains。 | FIX-20260628-061 |
| DQAF-20260622-060 | 2026-06-22 | Sev 2 | **PSI 方法论升级: Raw-Feature 假阳性→机构级双模漂移检测。** 归一化前 mean_PSI=2.73 (36/40 Sev1), 归一化后 mean_PSI=3.42 — 确认真阳性 regime change (BTC ATR 4→34~95)。5 工程契约: 零方差熔断 + 对数发散保护 + Mode B 自归一化 + 排他窗口隔离 + 样本非对称缓解。`stability_monitor.compute_psi()` → @deprecated, 统一为 `monitor_feature_drift._compute_psi()`。双模架构: Mode A (Regime vs Training) 触发重训, Mode B (Anomaly vs Rolling 7d) 触发告警。 | **CLOSED** — 投委会 5 指令全落实。Baseline `feature_baseline_v9_normalized_20260622.json` 已生成 (160,138 samples, norm μ/σ 内嵌)。 | FIX-20260622-060 |
| DQAF-20260623-065 | 2026-06-23 | Sev 1 | **MetaFilter Category Error — XAU Swing 被 StatArb MetaFilter 跨策略污染。** StatArb OU MetaFilter 路由所有 swing 策略 → p_win=0.193 → 交易全部被拒。ReB: STATARB_METAFILTER_CROSS_STRATEGY_CONTAMINATION。 | **APPROVED → CLOSED** — FIX-20260623-065: meta_filter_routing.py statarb 路由块切除 swing 策略名。 | FIX-20260623-065 |
| DQAF-20260623-066 | 2026-06-23 | Sev 1 | **p_win Cold-Start Triple-Break: -34.84R 36h (0% XAU WR, 13% BTC WR).** DQAF-065 后所有 swing → cold_explore_neutral → 盲 p_win=0.50 → 方向偏差无抵抗力。PnL store 重启为空, governance 数据未接入 p_win 链。ReB: COLD_EXPLORE_TRAP。 | **APPROVED → CLOSED** — FIX-20260623-066: P0-1 governance fallback, P0-2 cold_explore→governance, P0-3 ≥2 LIVE brains gate。 | FIX-20260623-066 |
| DQAF-20260623-070 | 2026-06-23 | Sev 2 | **Ghost Field `_feature_buffers_warm` — Silent Fail-Open AttributeError in session_guards.** LiveCycleState 缺少字段→每周期AttributeError→被过宽 except 吞没→冷特征裸奔。 | **APPROVED → CLOSED** — FIX-20260623-070: 补齐字段+MT5 bootstrap后置True+getattr安全访问+异常处理分层(状态完整性→fail-closed)。 | FIX-20260623-070 |
| DQAF-20260623-071 | 2026-06-23 | Sev 3 | **FeatureService 300s SLA Boundary Collision — 缓存边界竞争。** 特征持久化间隔(~300s)恰好等于新鲜度SLA(300s) → 缓存频繁在新鲜/过期临界翻转 → 不必要Tier 2实时计算。 | **APPROVED → CLOSED** — FIX-20260623-071: SLA 300→310s (负向抖动余量), 真正的管线冻结产生600s+ age。 | FIX-20260623-071 |
| DQAF-20260623-072 | 2026-06-23 | Sev 1 | **DQAF-059/066 Governance Gate Silent Nullification — Wrong Dict Level.** `strategy_line.py:538` 遍历 `governance_state.items()` (顶层键) 而非 `governance_state["brain_states"].items()` → `_live_brain_ids` 恒为空 → DQAF-059/066 自部署以来全部静默失效。 | **APPROVED → CLOSED** — FIX-20260623-072: `governance_state.get("brain_states",{}).items()` 单行修复, 所有下游消费者自愈。 | FIX-20260623-072 |
| DQAF-20260622-058-bis | 2026-06-22 | Sev 2 | **MetaFilter `micro_scaler_loaded: false` 23 天根因: 第 4 站点 `meta_signal_filter.py:135` 仍使用 `joblib.load()`。** DQAF-054/055 修复了 3 个站点但遗漏 MetaFilter — 此处是 `MicrostructureFeatureAdapter` 的独立实例化。附加修复: `live_intent_loop.py` + `bootstrap_v9.py` 缺少 `resolve_scaler_path()` 回退。 | **CLOSED** — `self._micro_scaler = joblib.load(path)` → `MicrostructureFeatureAdapter._load_scaler_json(Path(path))`。`live_intent_loop.py:1512` + `bootstrap_v9.py:91` 新增回退路径。 | FIX-20260622-058-bis |
| DQAF-20260622-058 | 2026-06-22 | Sev 2 | **PSI 特征漂移歼灭: Micro Scaler 全链路 5 站点修复 + 部署验证。** `micro_scaler_loaded: false` 持续 23 天 — 所有 9 个 microstructure 特征在 raw 空间运行 → PSI 无法准确测量。`resolve_scaler_path()` 替换 `btc_micro_scaler.json` 硬编码, XAU 冷启动 identity scaler 生成, `check_meta_filter_state` 新增 `micro_scaler_loaded` 追踪, `MICRO_SCALER_NOT_LOADED` 警告码。多品种 scaler 生成脚本 `generate_micro_scaler.py` 替代 BTC-only 脚本。 | **CLOSED** — `data_xau/models/xau_micro_scaler.json` (cold-start identity) + `generate_micro_scaler.py` (multi-asset). XAU 实证 scaler 待 Feature Store ≥5000 记录 → deferred. | FIX-20260622-058 |
| DQAF-20260622-057 | 2026-06-22 | Sev 2 (复合) | **Label Coverage 退化 (XAU 65%, BTC 40%) + Journal Timestamp Inversions 恶化 (XAU 336→365, BTC 22→52)**。根因: (A) 66% DEAL_REASON_SIGNAL 外部平仓 → journal 缺失 close_price → label_builder 无 defense layer → unlabeled。(B) live_cycle.py:1338 `.locks` vs `locks` 锁命名空间碎裂 → FIX-077 修复不完整 → inversions 继续恶化。(C) mt5_bridge_worker overflow merge 零锁写入。ReB: ReB-20260622-LABEL_COVERAGE_DEGRADATION。 | **CLOSED** — Phase 1 (37cc04dd): P0-1 (.locks→locks), P0-2 (overflow FileLock), P0-3 (label_contract injection)。Phase 2 (27ec65e): A1 per-brain contract resolution (24 brains), A2 journal PnL fallback, A3 contract-aware classification + label_source, B1 behavioral diversity gate (r≥0.95 WARN), C1 label_coverage_pct, C2 verify.py regression gate。Coverage: BTC 40%→93.1%。Phase 3a (32e62aa5): full forward-walking barrier simulation (XAU 429, BTC 29 records)。Phase 3c (TBD): journal PnL backfill + .locks residual fix — lock namespace fully unified。 | FIX-20260622-057 |
| DQAF-20260622-051 | 2026-06-22 | Sev 2 | **Train-Serve SL/TP 校准鸿沟: 4 大脑缺失 label_contract, BTC_Swing_V4 SL 偏差 33% (1.5→2.0)**。verify.py 只能 WARN 无力阻止。V10_H1_Directional 额外发现: SL/TP 完全倒挂 (训练 2.0/3.5 vs 实盘 3.0/2.0)。 | **CLOSED** — Phase 1: 4 brains + label_contract, BTC_Swing_V4 live_btc.yaml SL 2.0→1.5 + TP 2.5→1.5 (实盘对齐训练)。V10: 新建 h1_directional 策略线 (SL=2.0/TP=3.5)。Phase 2: verify.py WARN→FATAL ERROR + SL/TP mismatch detection。 | FIX-20260622-051 |
| DQAF-20260622-050 | 2026-06-22 | Sev 2 | **Cold-Start Double Deadlock: Alpha Promotion Gate 与 Alpha Feed 度量体系完全不兼容 (L3 架构死锁)**。Promotion gate 要求执行度量 (signal_count, fill_ratio, order_count) 但 alpha_feed 产出的是 PnL 度量 (trade_count, win_rate, profit_factor) — 零重叠。所有 candidate 大脑永久卡死。即使晋级, allocator 也因无 AlphaPerformanceSnapshot 而阻塞 (双重死锁)。 | **CLOSED** — Route B+: (1) 治理快速通道 (governance fast-track) — live/probation 大脑跨过冷启动障碍, (2) 冷启动快照回填 — 从 leaderboard+governance 创建初始性能快照激活分配器 PnL 回退路径, (3) 数据完整性修复 (strategy_class + assets + ghost cleanup)。结果: 5 brains→probation_live, allocatable_count 0→5。Phase 3 (阶段感知门禁) 延后。 | FIX-20260622-050 |
| DQAF-20260622-049 | 2026-06-22 | Sev 2 (含 Sev 1 CLI 后门) | G3 Alpha Allocation 绝对真空: 0/69 大脑获得资金配额。Brain Governance 与 Alpha Pipeline 之间缺少提名桥接 (RC-12)。附加发现: CLI 使用裸 json.dumps() 绕过 StateWriter 门禁 (Sev 1)。 | **CLOSED** — Phase 1: 数据完整性修复 + CLI 后门焊死。Phase 2: Leaderboard → Alpha Registry 提名桥 (3 brains nominated)。 | FIX-20260622-049 |
| DQAF-20260622-048 | 2026-06-22 | Sev 2 | XAU Leaderboard 95.7% 决策记录不可见 (glob XAUUSD vs XAUUSDc 不匹配) + POISON PILL 被外层 except Exception 架构性击败. ReB: SYMBOL_AWARE_DECISION_FILE_NAMING. | **CLOSED** — Dual fix: symbol-aware glob + POISON PILL enforcement + ratio-based contract validation. | FIX-20260622-048 |
| DQAF-20260621-044 | 2026-06-21 | Sev 2 | BTC ConformalCalibrator p_win 塌缩为 0.5 (诊断正确, 修复不完整). ReB: COLD_EXPLORE_PREEMPTIVE_OVERRIDE. | **CLOSED → SUPERSEDED by DQAF-20260621-044-bis** — 原始修复仅覆盖 MetaFilter PASS 分支 (_meta_p_win is not None), 遗漏 REJECT 分支 (96.3%). | FIX-20260621-044 → FIX-20260621-044-bis |
| DQAF-20260621-044-bis | 2026-06-21 | Sev 2 | DQAF-044 修复不完整: 重启后 p_win 仍 100%=0.5. 根因: _is_cold_explore 清除条件仅覆盖 MetaFilter PASS, REJECT 时 _meta_p_win=None 导致修复不触发. IC 否决 Tier 1 补丁, 强制 Tier 2 (TEMPORAL_STATE_DECOUPLING: 状态后置推导) + Tier 3 (journal p_win_source 可信锚). | **APPROVED → CLOSED** — FIX-20260621-044-bis: 以 MetaFilter 实际返回值推导 _is_cold_explore (不再先设后清). 毒流代谢期 ~50 trades. ReB: TEMPORAL_STATE_DECOUPLING (BooleanStateDesync 升格). | FIX-20260621-044-bis |
| DQAF-20260610-001 | 2026-06-10 | Sev 2 | BTC 移动止损修改前后对比: 0%胜率伪影→数据证伪→亏损来自改前旧仓位(trail卡死)，改后trail激活(+768pts)保本防守。根因非trail而是微生命周期+全long逆势+遥测盲区。 | **APPROVED → CLOSED** — IC Mandate: 冻结trail参数(衰减曲线已验证正确)，立即修复MIA管道PnL缺失(close_accepted/breakeven无PnL)。ReB: TRAIL_TELEMETRY_BLINDSPOT + MICRO_LIFESPAN_COUNTER_TREND | FIX-20260610-004, FIX-20260610-006, FIX-20260610-007 |
| DQAF-20260612-001 | 2026-06-12 | Sev 3 | data_health CRITICAL: journal PnL null 17.6% + dupes 89 + trail label 0 + calibrator cold-stalled + 位置脆弱性 .values() 5站点. 6源证据→5根因→5 FIX | **APPROVED → CLOSED** — FIX-002 (位置脆弱性→命名投影), FIX-003 (P0洪水锁+P1 trail标签), FIX-004 (P2 bridge PnL+P4 MIA重试), FIX-005 (P5 calibrator cold_started过渡). ReB: PHANTOM_CLOSE_FLOOD + TRAIL_LABEL_BLINDSPOT + PNL_BACKFILL_GAP + CALIBRATOR_COLD_STALLED + POSITIONAL_FRAGILITY | FIX-20260612-002, FIX-003, FIX-004, FIX-005 |
| DQAF-20260612-002 | 2026-06-12 | Sev 1 | no_live_brains 全交易阻塞: FIX-20260610-001 退役 V5 三处残留(status=retired, vote_weight=0, enabled=false)与 governance 升为 live 冲突 → _live_count=0 → 所有周期降级。三重修复+Cut 4 SSOT重构。 | **APPROVED → CLOSED** — IC Mandate: 三步同步 (registry→live + weight→1.0 + yaml enabled→true) + strategy_evaluator Cut 4 decision.brain_ids SSOT 替换 strategy.brains 嵌套 dict. ReB: TRIPLE_BOOKKEEPING_RESIDUAL + GOVERNANCE_BRAIN_SOURCE_MISMATCH | FIX-20260612-006 |
| DQAF-20260612-004 | 2026-06-12 | Sev 2 | KI-004 收口: resolve_p_win_from_brains() 三条静默 fallback 路径全部返回 0.40, 无日志无告警. p_win=0.40 在 journal 中占比 0% (已有隐式安全网) | **APPROVED → CLOSED** — FIX-20260612-001: Phase 0 可观测性注入. BLE001→fail_open_guard + 3条结构化告警 + p_win_source/p_win_degraded 透传至 journal. ReB: SILENT_FALLBACK_ZERO_OBSERVABILITY | FIX-20260612-001 |
| DQAF-20260610-002 | 2026-06-10 | Sev 2 | 入场/出场全量审计: V9/V10 Survival brain SL/TP训练-部署偏差, BTC_Swing_V5退休后残留在XAU配置, kalman_velocity/meta_exit等10+出场模式未分类→标签中毒 | **APPROVED → CLOSED** — 4项修复: (1) XAU配置清理V5, (2) V9/V10补全label_contract(SL=3.0/TP=2.0生存模式,需专属策略线), (3) verify.py新增`_check_config_consistency()`跨品种污染+退役大脑+contract缺口静态闸门, (4) `_classify_exit_reason()`补全14种出场模式. 虚拟沙盒推迟(独立项目). ReB: CONFIG_SYMMETRY_DRIFT | FIX-20260610-008 |
| DQAF-20260609-012 | 2026-06-09 | Sev 2 | BTC 大脑盈利能力诊断: V5 test_PF=1.81→live_PF=0.73, 归一化器XAU复制品, M5/12bar信号≈随机. 全TF网格搜索→BTC不支持R:R≥1.0. M15 SL=3.0/TP=2.0 EV=+0.456R 全场最佳. | **APPROVED → CLOSED** — FIX-20260609-012: B1审计→B2/B3重训管线→V9 H1 (EV+0.38R) + V10 M15 (EV+0.46R) shadow注册. ReB: BTC_SURVIVAL_ALPHA | FIX-20260609-012 |
| DQAF-20260609-011 | 2026-06-09 | Sev 2 | 大脑治理真空: 4 candidate brains 零 live, 全票权开单, 0.1 lot 裸奔, PnL ~-$30/day. candidate 倒挂 (全票权) vs probation (0.5×). | **APPROVED → CLOSED** — FIX-20260609-011: (1) candidate vote_weight×0.5, (2) governance_state per-cycle 管线, (3) strategy_evaluator 无 live 降级 (conf<0.50→blocked, vol→0.01). ReB: GOVERNANCE_VACUUM_CADET_BRAINS | FIX-20260609-011 |
| DQAF-20260609-002-UPDATE | 2026-06-09 | Sev 2 | 剥洋葱排查: BTC MetaFilter模型文件根本不存在(configs/brains_btc/),静默退化到rolling_wr(0.48),跨品种盲点模式 | **APPROVED → CLOSED** — FIX-002-UPDATE (Hard Floor Defense) + FIX-002-BTC (Path A: BTC MetaFilter V1训练, 47-dim LGB, val WR 70.9%). Path B (≥200笔实盘重训) 搁置. ReB: CROSS_SYMBOL_METAFILTER_BLINDSPOT | FIX-20260609-002-UPDATE, FIX-20260609-002-BTC |
| DQAF-20260609-002 | 2026-06-09 | Sev 2 | XAU 2日交易退化: WR=20%, h1/h4 p_win=0.41(rolling_wr)绕过MetaFilter, 三swing同向同时开仓, BrainSignal合约断裂37次/9天 | **APPROVED → CLOSED** — FIX-20260609-002: (1) h1/h4接入MetaFilter, (2) Kelly低RR保本线兜底, (3) Family同周期乐观锁, (4) BrainSignal兼容. ReB: ReB-20260609-002 | FIX-20260609-002 |
| DQAF-20260609-001 | 2026-06-09 | Sev 2 | BTC 23h 零开仓 + 全栈健康检查: (A) hesitation reentry 数学死锁 150 cycles, (B) budget 每 cycle 归零 → 全部累计风控失效, (C) 大脑治理真空, (D) MIA/孤儿/仓位放大 | **APPROVED → CLOSED** — FIX-20260609-001: `_MAX_THRESHOLD` 天花板 + TTL 硬解锁. FIX-20260609-010: (1) budget 每 cycle 从磁盘恢复, (2) hesitation margin +0.15→+0.08, floor 0.70→0.65. ReB: Cap-Output Mismatch Deadlock + Budget Reconstruction Amnesia | FIX-20260609-001, FIX-20260609-010 |
| DQAF-20260608-003 | 2026-06-08 | Sev 2 | 熔断器碎片化 trip 路径：6条独立路径×3种计数器，auto-reset仅清除1种→stale counter泄漏→立即重新trip→死亡螺旋。系统May 31重启110次。 | **APPROVED → CLOSED** — FIX-20260608-009: (1) 5条trip路径全部记录 `trip_reason`，(2) auto-reset统一清除全部3种counter，(3) 持久化补齐全部counter+trip_reason，(4) restore防幽灵breaker。ReB: `FRAGMENTED_BREAKER_TRIP_PATHS_WITH_STALE_COUNTER_LEAK` | FIX-20260608-009 |
| DQAF-20260608-001 | 2026-06-08 | Sev 1 (复合) | 全量数据异常审计: 熔断器永久卡死(Sev 1) + calibrator时间戳损坏(Sev 2) + MetaFilter静默失效(Sev 2) + Alpha空跑(Sev 2) + Golden Master仅3天(Sev 3) — 10项异常收敛为5个独立根因 | **APPROVED → CLOSED** — FIX-20260608-003: (1) 熔断器冷却制统一自愈, (2) MetaFilter模型路径修复, (3) calibrator时间戳字段-值错配修复. ReB: CIRCUIT_BREAKER_RESET_ASYM, ORPHAN_SUBSYSTEM_DETECTION | FIX-20260608-003 |
| DQAF-20260606-004 | 2026-06-06 | Sev 2 | 6+ 小时零开仓——p_win=0.44 与 breakeven=0.45 之间的 0.01 死锁带，双闸门交替拦截 (p_win + bleed_stop) | **APPROVED → CLOSED** — UCB 弹性地板 (FIX-139) 填平死锁带。置信度推导 p_win=0.482，Kelly 自动微仓探索。方案三优于方案一二。 | FIX-20260606-139 |
| DQAF-20260626-001 | 2026-06-26 | Sev 2 | **Golden Master 5 天静默缺口 + regime_snapshots 级联故障。** BLE001 Phase 3a 收窄异常处理→AttributeError 逃逸+golen_master.py blind except:pass 零日志→06-20~06-24 零条 GM 记录→regime_snapshots 同步停滞。ReB: BLE001_NARROW_CATCH_CASCADE。 | **APPROVED → CLOSED** — FIX-20260626-001: 防御深度加固。(1) golden_master.py: regime_info.get() 内部防御 AttributeError + blind pass→fail_open_guard。(2) live_cycle.py: try/except→fail_open_guard。(3) regime_snapshots 从 GM+FS 双源重建 (BTC 8293, XAU 54623)。 | FIX-20260626-001 |
| DQAF-20260606-003 | 2026-06-06 | Sev 3 | 重启后立即开单——排查是否 FIX-137 引入回归 | **APPROVED → CONFIRMED** — 老问题重现 (RC-03 state-leak)，cooldown 清理非致因。关联已知存量模式 `state_leak_across_restart` | — |
| DQAF-20260607-005 | 2026-06-07 | Sev 1 | UnboundLocalError 导致 dispatch 崩溃 → 孤儿持仓 → 系统数小时只开仓不平仓 (Fail-Open) | **APPROVED → CLOSED** — 三层防线: FIX-140 (Fail-Closed dispatch), FIX-141 (孤儿富化), FIX-142 (兜底网关) | FIX-20260607-140, FIX-20260607-141, FIX-20260607-142 |
| DQAF-20260607-007 | 2026-06-07 | Sev 3 | 架构师提案: 趋势衰竭 Confidence Decay + V型反转非对称出场。诊断确认: 拒绝N笔交易计数衰减，接受 Kalman+Hurst 状态驱动仓位缩放 + Kalman 速度翻转快速出口。 | **APPROVED → CLOSED** — `trend_maturity_discount()` + `evaluate_brain_exit()` Kalman velocity flip 接线完成。纯增量，两个信号已计算仅未消费。 | FIX-20260607-143 |
| DQAF-20260608-002 | 2026-06-08 | Sev 2 | XAUUSDc meta_exit 平仓无钉钉通知: `dispatch_managed_close()` 遗漏 `notify_trade` 调用, 所有受管平仓静默 | **APPROVED → CLOSED** — FIX-20260608-005: 在 `dispatch_managed_close()` 尾部注入 fire-and-forget `notify_trade(action="close", ...)`. 覆盖全部 7+ 受管退出路径. ReB: MISSING_NOTIFY_IN_MANAGED_CLOSE | FIX-20260608-005 |
| DQAF-20260620-002 | 2026-06-20 | Sev 2 | PnL量纲混合: budget_breached误触发—$5亏损被计为-500%日PnL。三条代码路径(raw USD→decimal pct)量纲不匹配,仅 reconciliation 路径正确 | **APPROVED → CLOSED** — FIX-20260620-003: (1) position_close_adapter 移除冗余 _notify_budget 回退路径, (2) live_cycle MIA 路径 USD→pct 转换, (3) managed_close 硬编码 /1000→实时 equity。ReB: PNL_UNIT_MIXING | FIX-20260620-003 |
| DQAF-20260615-012 | 2026-06-15 | Sev 2 | XAU 告警胜率崩塌: 752条 auto_orphan_ 合成条目(pnl=0,无ticket)污染滚动窗口→WR=0.91%→断路器误断开。根因是告警上下文未区分真实交易与 orphan 合成 close | **APPROVED → CLOSED** — live_cycle.py alert context 新增 auto_orphan_ label 过滤。修复前 XAU WR=0.91%, 修复后 WR=46.67%。0 行实盘逻辑变更(纯告警展示层)。ReB: ORPHAN_ENTRY_ALERT_POLLUTION | FIX-20260615-012 |
| DQAF-20260615-011 | 2026-06-15 | Sev 2 | 钉钉告警"最差大脑"PnL与实盘事实不符: 退役大脑V11幽灵(-1452.68 R)永久霸占最差位置, get_all_metrics()未过滤活性, load_from_stream() R-multiple/dollar量纲混乱, _hydrate_accumulators()读错误字段 | **APPROVED → CLOSED** — 三重修复: (1) load_from_stream pnl_r→pnl_per_unit逆向还原, (2) _hydrate_accumulators 字段名+时间戳修正, (3) live_cycle 告警上下文新增 governance 活性过滤。修复后 V4(WORST_ACTIVE) 取代 V11(GHOST)。ReB: ARCHIVED_BRAIN_ALERT_POLLUTION | FIX-20260615-011 |
| DQAF-20260621-033 | 2026-06-21 | Sev 2 | **66%仓位在系统控制外平仓 (99/150): mt5_deal_reason_3=DEAL_REASON_SIGNAL** — 时序分析证伪 double-journaling (0/99 matched pairs ±5s)。H2(经纪商自动化平仓)解释度 100%。P0热补丁 FIX-035 (deal_reason溯源+MAP统一) + P1 FIX-036 (position_identifier 两路径注入对账主键) 已部署。 | **APPROVED → P0+P1 DEPLOYED** — IC Mandate 签发, FIX-035 + FIX-036 deployed. P2 (经纪商账单导出) 待执行 | FIX-20260621-035, FIX-20260621-036 |
| DQAF-20260621-034 | 2026-06-21 | Sev 2 | **Trailing SL 48.7%仓位零快照 + 46.3%有快照trail未移动 = STATE_INITIALIZATION_DEADLOCK** → FIX-037 三刀热补丁 + FIX-038 两刀架构修复 + FIX-039 L3 架构收敛 (消除冗余 per-cycle sync)。全链路: 止血→序列化补全→不可变锁→架构收敛。 | **APPROVED → CLOSED** — FIX-037/038/039 deployed, 架构债清偿 | FIX-20260621-037, FIX-20260621-038, FIX-20260621-039 |

| DQAF-20260621-042 | 2026-06-21 | Sev 1 (复合) | **机构级实盘数据方向全面普查 — 10项发现对账确认8项坐实。** Leaderboard崩溃(Sev 1), Journal vs Labels 38%缺口(Sev 2), 治理含3140笔回测数据(Sev 2), 校准器p_win退化至0.5(Sev 2), brain_perf维度为空(Sev 3), Alpha数据不一致(Sev 2), 健康报告自相矛盾(Sev 2), golden_master未排序(Sev 3), 修复被实盘进程覆盖(Sev 1新发现)。根因: IMMUTABLE_LEDGER_AND_EPHEMERAL_PROJECTION 架构反模式。 | **APPROVED → CLOSED** — 四防线全生命周期竣工: P0毒丸阻断 (7d448ae) → 防线1物理隔离 (4642137) → 防线2契约测试26/26 (f867b80) → 防线4 AI护栏 (4642137) → 蓝图终更 (e8fe77c5)。P1-B(purge执行)由DQAF-043完成。CCT-042已回填, ReB-042已注册。P1-A(统一标签出口)转入deferred。 | FIX-20260621-042 |
| DQAF-20260622-047 | 2026-06-22 | Sev 3 | **TRAINING_READINESS 管线断裂 — BTC 168h stale, XAU missing。** 根因: check_training_readiness.py 是完整可工作的 CLI 脚本但从未被 daily_ops.py 调用 (orphan generator)。修复: (1) 提取 `evaluate_training_readiness()` 纯函数, (2) 野生 json.dump()→StateWriter, (3) daily_ops 新增 `_step_training_readiness()` 带符号隔离前缀匹配。Freshness Guard CRITICAL→CLEAN。ReB: ORPHAN_GENERATOR_NOT_WIRED_TO_PIPELINE。 | **APPROVED → CLOSED** — FIX-20260622-004 deployed。 | FIX-20260622-004 |
| DQAF-20260621-046 | 2026-06-21 | Sev 2 | **XAU 信号生成真空 — 45 天零决策。** 双根因: (1) BrainSignal API fracture — `signal.prediction` dict 被 `signal.direction` Literal 替换, live_shadow_ensemble 仍用旧接口读取 → 所有脑返回 neutral。(2) Feature dimension mismatch — 35-dim swing brains 收到 40-dim institutional v9 特征, 完全不同特征空间。双轨特征流水线修复: 35-dim swing_enhanced_35 特征解析器 (DailyFeatureComputer 24 daily + 9 micro + 2 TF) + 特征路由器 (feature_schema_id) + 16 brain configs 注册 schema。0/21→11/21 non-neutral signals。ReB: FEATURE_SCHEMA_ROUTING_AND_BRAIN_API_CONTRACT。Plan B (State Governance Protocol) 同步交付——4层防线: Data Catalog + Write Gate + Cross-Symbol Guard + Freshness Guard。 | **APPROVED → CLOSED** — FIX-20260622-003 (XAU dual-track) + FIX-20260622-001 (Plan B Phase 1-4)。 | FIX-20260622-003, FIX-20260622-001 |
| DQAF-20260625-125 | 2026-06-25 | Sev 2 | **XAU leaderboard 3956min stale — watchdog_daily_ops 三连盲。** 根因: FIX-20260622-001 (Plan B StateWriter迁移) 遗漏 `watchdog_daily_ops.py`。三处 bug: (1) 路径 `daily_ops_state.json`→`state/daily_ops_state.json`, (2) 字段名 `last_run_utc`→`last_daily_ops_utc`, (3) never-run 分支未实现 auto_run。watchdog 完全盲化 — 即使 `--auto-run` 也不执行。Leaderboard 手动恢复: XAU 69 brains 1192 decisions。ReB: ORPHAN_WATCHDOG_MIGRATION_SWEEP_INCOMPLETE。 | **APPROVED → CLOSED** — FIX-20260625-125 deployed。三处修复: 路径+字段名+auto_run。MODULE_SOURCE_MAP 注册 watchdog_daily_ops.py + daily_ops_scheduler.py → runtime_live。 | FIX-20260625-125 |
| DQAF-20260621-043 | 2026-06-21 | Sev 2 (复合) | **DQAF-042 竣工后机构级复检 — 7项新发现。** 治理周期静默崩溃6+天(Sev 1): FIX-032 journal增强 dict→dataclass 类型不匹配+except Exception吞没→_step_governance每日crash。双品种14 brains回测污染(Sev 2)。32% label gap+Bridge-Journal ticket命名空间断裂(Sev 2)。快照覆盖50-82%缺失(Sev 2)。大脑治理-日志数量断裂(Sev 2)。校准器双品种全死(Sev 2)。XAU 45笔未知大脑+85.74R无归属(Sev 3)。 | **APPROVED → CLOSED** — P0: 新增_dict_to_pnl_metrics()类型转换器+增强后类型断言+_data_source审计标签。P0: daily_ops错误处理记录异常类型。P1: 3合约测试(类型转换+缺字段+完整周期)。P1-B: purge脚本双品种执行: BTC 1 brain修正(X12: 3140→41 trades), XAU 13 brains修正。ReB: BOUNDARY_TYPE_ENFORCEMENT_AND_EXPLICIT_CATCH。Finding C/D/F分别开具独立DQAF Dockets。 | FIX-20260621-043 |

| DQAF-20260715-011 | 2026-07-15 | Sev 2 | **Counter-trend gate L3 设计缺陷 — `_is_cold_explore` 豁免使所有 probation 策略绕过趋势对齐闸门。** H4 trend=long/strength=0.6 时仍开 SHORT, -$51.54 实盘亏损。L2: 多 TF 策略名 (btc_swing_h4) 不匹配 threshold dict key (btc_swing) → 落入宽松 default。 | **APPROVED → CLOSED** — FIX-20260715-011: (1) strategy_line.py 移除 cold_explore/governance_guided 豁免 + BLE001 fail-open guard, (2) penalise 惩罚乘法叠加 (0.5×0.70=0.35), (3) trend_volume_guard.py 前缀匹配策略名, (4) BTC_Swing_M15 retired, (5) live_btc.yaml M15 disabled。ReB: COLD_EXPLORE_GATE_EXEMPTION | FIX-20260715-011 |

## 裁决状态说明

- `AWAITING_IC` — 已提交 [DQAF_REPORT]，等待人类 IC 审批
- `APPROVED` — IC 批准，进入修复阶段
- `REJECTED` — IC 驳回，需重新诊断（注明驳回原因）
- `TIERBREAKER` — DA 与 AR 结论矛盾，IC 要求第三轮诊断
- `CLOSED` — 修复完成并验证通过

---

## DQAF-20260722-002

- **Date**: 2026-07-22
- **Severity**: Sev 2
- **Title**: Exit label reform + p_win small-N degradation (P0+P2 投委会令)
- **Evidence**: Terminal Epoch analysis (7/15-7/22, _analyze_xau_institutional.py); 8/9 give-back positions labeled "loss" with zero provenance
- **DA (Direct Analysis)**: P0: PnL-based labeling at position_close_adapter.py:443-446 discards exit signal from deal comment. P2: resolve_p_win() rolling_wr path uses any N without degradation; Terminal Epoch rolling_wr WR=41.2% vs brain_confidence WR=72.4%.
- **AR (Adversarial Refutation)**: P0 not MIA close (0 MIA markers in 7/15+ window). P2 not governance stale (brain_confidence WR=72.4% well-calibrated).
- **Root Cause**: L2 — P0: telemetry poisoning (PnL-based label discards causal signal). P2: small-sample noise injection (N<30 rolling_wr is random walk).
- **Fix**: FIX-20260722-002 — P0: position_close_adapter.py, reconciliation.py → structured `managed:<signal>` / `broker:<reason>`. P2: pwin_chain.py → N<30→brain_confidence, 30≤N<50→blend.
- **Status**: **CLOSED** — committed + pushed (95446421)

## DQAF-20260722-003

- **Date**: 2026-07-22
- **Severity**: Sev 2
- **Title**: Cross-TF trail activation mismatch (P1 投委会令)
- **Evidence**: Ticket 4207155654 forensic trace (138 snapshots, full journal lifecycle); +6.03R entry_atr → 0.54R bracket_atr < trail_activation_atr=1.0
- **DA**: trail_stop_engine.py:227-233 activation watermark uses _resolve_geometry_atr() → bracket_atr (H4 ~57) but ratchet uses entry_atr (M5 ~5.1). For H1/H4 strategies where bracket_atr >> entry_atr, trail_activation_atr=1.0 is effectively unreachable.
- **AR**: Not cold_explore (kelly_mult=1.03). Not min_hold_cycles (bar 10 > 3). Not exit_min_step (difference 124.65 >> 0.5).
- **Root Cause**: L2 — cross-TF geometry ATR mismatch: activation uses bracket_atr, ratchet uses entry_atr, snapshots use entry_atr. Three different R-measurement scales in one pipeline.
- **Fix**: FIX-20260722-003 — trail_stop_engine.py: activation watermark + breakeven now use pos.entry_atr (consistent with ratchet and snapshots).
- **Status**: **CLOSED** — committed + pushed (95446421)

- **Docket ID**: DQAF-20260730-011
- **Date**: 2026-07-30
- **Severity**: Sev 1
- **Title**: Journal PnL SSOT缺失 — 双写竞争+修正链路饥饿（Settlement Queue Isolation）
- **Evidence**: BTC七月: MT5 +$26.86 vs 系统journal -$140.89（偏差+$167.75）。1226条close中1217(99.3%)来自Bridge，仅9(0.7%)来自Reconciliation。796条(65%)无_pnl_status provenance标签。Bridge在order_send()后立即history_deals_get()→MT5 deal清算未完成→回退到引擎中价估算(mid-entry)*volume→静默写入journal无provenance。
- **DA**: Journal PnL字段无Single Source of Truth。Bridge在deal.profit不可用时使用msg_payload["pnl"](中价估算)。Reconciliation通过resolve_exit_deal()正确策展DEAL_ENTRY_OUT但仅写9条因known_open_tickets在reconciliation运行前被清除。
- **AR**: 推翻"漏记交易"(票号100%重叠)、"commission/swap遗漏"(BTC账户comm=$0, swap=$0)、"Reconciliation会修正"(修正链路饥饿,仅0.7%)。
- **Root Cause**: L3 — Journal PnL字段无SSOT。C1: MT5 deal异步清算窗口→Bridge读不到deal.profit。C2: Bridge静默回退到中价估算。C3: 修复前65%条目无provenance标签。C4: Reconciliation修正链路饥饿(known_open_tickets提前清除)。C5: Dedup允许reconciliation supersede但饥荒。
- **Fix**: FIX-20260730-011 — Settlement Queue Isolation (L3架构修复): (1) Bridge deal.profit不可用时写pnl=null+_pnl_status="pending_mt5_settlement",估算值仅存detail.estimated_pnl供取证。(2) SettlementQueue(新模块core/runtime/settlement_queue.py)实现known_open_tickets→pending_settlement_tickets→settled三态隔离。(3) Reconciliation作为唯一PnL权威消费pending_settlement_tickets,通过resolve_exit_deal()轮询直到deal.profit可用→写journal(_source=mt5_reconciliation自动supersede bridge的pnl=null条目)。(4) 四级超时上报(Tier1 5min→Tier2 1hr→Tier3 24hr降级写入→Tier4 24hr终端超时),队列持久化+僵尸单告警。
- **ReB**: JOURNAL_PNL_DUAL_WRITER_RACE_WITH_STARVED_CORRECTION_PATH
- **Status**: **CLOSED**

- **Docket ID**: DQAF-20260801-010
- **Date**: 2026-08-01
- **Severity**: Sev 2
- **Title**: BTC_Swing_V4 live↔probation 振荡 — 双轨写入器 + V4 政策冲突
- **Evidence**: V4 自 07-09 起 live↔probation 振荡。三个写入器竞争: (1) live_intent_loop `apply_promotion_decisions()` (BrainPnLStore last-20, FIX-20260611-001 补丁, 每 50 周期); (2) daily_ops governance_scheduler.py:664 直写 `governance.transition()` (BrainPnLStore all-time, 每周期); (3) 启动 reconcile (config floor)。SSOT 干跑 (brain_performance window-100: V4 41W/59L PF=0.695) 输出 `throttle live→probation reasons=['profit_factor(0.69) < 0.80']`。
- **DA**: 6 源 ECoL (live_intent_loop.py:2470 块 / governance_scheduler.py:664 / brain_performance window-100 / governance_state 转场日志 / configs/brains_btc/BTC_Swing_V4.json / FIX_REGISTRY FIX-20260611-001)。干跑验证证明: 即使用唯一 SSOT 数据源, V4 也被 policy-correctly throttle (PF=0.695 < 0.80)。
- **AR**: 推翻"双轨数据源是唯一根因" — 单 SSOT writer 干跑仍产生 throttle。真因是**政策冲突**: BrainPromotionEvaluator throttle (PF 风控) vs Iron Law #14 config floor (V4 config=live) vs daily_ops pnl:stable 拉回 (all-time 健康)。双轨写入器是帮凶非真凶。投委会终局裁决: 否决维持现状(C)与接受降级(B), 批准观察期豁免(A)。
- **Root Cause**: L3 — 政策对峙 (三套政策对同一大脑矛盾结论)。P1-P4 全量执行会"合法枪决" V4 (SSOT throttle → 永久 probation, 撤销 7/27 live 观察决定)。
- **Fix**: FIX-20260801-011 (SSOT 统一: governance_evaluator.py 共享 orchestrator + launcher 60s 治理线程 + live_intent_loop 双轨切除 + daily_ops 路由 execute_transitions + apply_promotion_decisions TODO) + FIX-20260801-012 (Observation Hold: `observation_hold_until` config 契约 + GovernanceRuleEngine 拦截器, V4 hold 至 2026-08-03T23:59:59Z)。
- **ReB**: POLICY_CONFLICT_THROTTLE_VS_CONFIG_FLOOR; DUAL_TRACK_WRITER_OSCILLATION
- **Status**: **CLOSED**

- **Docket ID**: DQAF-20260801-011
- **Date**: 2026-08-01
- **Severity**: Sev 2
- **Title**: XAU 特征管线冻结 + bridge_silence 假阳性 — 单一根因 (feed 停摆 → session-off 误判 → 熔断短路 → 特征冻结)
- **Evidence**: (1) XAU `data/feature_store/.../timeframe=M5/features.jsonl` 最后记录 `event_time=2026-08-01T00:41:25Z`, 当日仅 2 条 (正常周五 150+ 直到 20:55 UTC); (2) intent_20260801T081238Z.log: `bar_sync_synthetic=810/811`, phase 仅到 `2_reconcile_positions` (811×1_startup_bootstrap, 810×2_reconcile_positions), **从未到达 `4_feature_computation`**; `circuit_breaker_active=403`, `bridge_silence_degraded=111`, `circuit_breaker_bridge_silence_trip=37`; (3) bar_sync_events.jsonl: BAR_SESSION_OFF 08-01 共 1405 (07-31 正常日仅 111, 从 22:07 收盘后才开始); (4) bridge_20260801T004240Z.log: 00:42:40 重启后 MT5 initialized + OFI TickPoller started 但无真实 bar; (5) 07-31 正常日特征写入 04:10–20:55。
- **DA**: 根因链: 重启 (00:42:40, SIGINT 外部触发) 后 XAU MT5 tick/价格 feed 停摆 → 动态 tick 探针 (session_detector, TECH_DEBT-005) 将"feed 死亡"误判为"市场关闭" (risk_tier=off) → bar sync 走合成 bar (BAR_SYNTHETIC) → `_last_bridge_ack_time` 不更新 (ack 仅在 management_phase:1306 fetch_prices 成功 / :1312 mid>0 时更新) → bridge_silence > 600s → `_consecutive_degraded_cycles ≥ 3` → circuit_breaker_bridge_silence_trip → **management_only_mode 短路整个 alpha 路径** → Phase 4 特征计算永不执行 → feature store 冻结在 00:41:25 → 任何到达推理的 brain 都会用冻结特征打分 (投委会盲推恐惧)。
- **AR**: 推翻"特征写入器自身故障" — 特征写入块 live_cycle.py:3330 异常处理静默 (except...pass), 但根因是循环在 Phase 2 后被熔断短路, 从未到达写入块。推翻"纯周末伪警" — 08-01 是周五正常交易日 (市场 00:00–22:00 UTC), 且 07-31 盘中无 BAR_SESSION_OFF。推翻"桥接真断流" — 08-02 当前进程 mt5_bridge_health age 0.9s connected, 桥接进程存活。
- **Root Cause**: L2/L3 — (L2) liveness 探针 `_last_bridge_ack_time` 是 price-ack 代理, 无法区分"市场关闭"(良性)与"feed 死亡"(真故障); 且 ack 更新路径在下游, 被熔断短路后形成循环依赖 (breaker trip → management 跳过 → ack 不更新 → breaker 保持)。(L3) 动态 tick 探针将 feed 停摆误分类为 session off, 静默降级而非告警; FeatureService 兜底静默返回 last-known/zeros, 无硬失败。
- **Fix**: FIX-20260801-013 (StaleFeatureException 推理守卫 — 新模块 stale_feature_guard.py + FeatureService 时间戳跟踪 + live_cycle 转 management_only) + FIX-20260801-014 (reconcile retired-as-ceiling, 15 幽灵状态清理, 独立异常但同 docket 关联)。
- **ReB**: FEED_STALL_MISCLASSIFIED_AS_MARKET_CLOSED; LIVENESS_ACK_CIRCULAR_DEPENDENCY
- **Status**: **CLOSED**

- **Docket ID**: DQAF-20260802-002
- **Date**: 2026-08-02
- **Severity**: Sev 2
- **Title**: Zero-vote brain pollutes ensemble p_win pool — Voting Boundary != EV Boundary
- **Evidence**: btc_swing trio cold_explore p_win 锚定 0.4141 = BTC_Swing_V4_LGB 治理 WR (probation, vote_weight=0.0)。`scripts/_audit_pwin_routing_20260802.py` 实证 TRIO 中位池含 V4_LGB 0.4141; live_launcher_20260802T021000Z.log 每周期负 EV 拦截; data_btc/governance_state.json V4_LGB vote_weight=0.0。
- **DA**: `pwin_chain.resolve_p_win_from_brains()` 治理冷启动池与 PnL 池仅按 live_brain_ids 过滤, 未过滤 vote_weight<=0 的 mute 脑 → 其历史 WR 入中位池 → "短板穿透" (weakest-link) 锚定 ensemble EV 下限。
- **AR**: 推翻"0.5 是配置值"重诊 — vote_weight 双层语义 (config SSOT vs 治理惩罚派生) 已由 FIX-20260731-004 处理; 本 docket 对象是显式 0.0 (contract-muted), 非 0.5 惩罚。
- **Root Cause**: L3 — 架构边界错误: Voting Boundary != EV Boundary。vote_weight<=0 的脑不能投票却仍贡献 EV 估计。
- **Fix**: FIX-20260802-001 — `_has_voting_rights()` (fail-open, 镜像 count_valid_voters) 过滤双池 (PnL 池 + 治理冷启动池) + `_compute_live_sample_total`。新池 = median([0.41, 0.4948]) = 0.4524。IC 终局裁决: Voting Boundary == EV Boundary。
- **ReB**: ZERO_VOTE_WR_POOL_PENETRATION
- **Status**: **CLOSED** — committed 58b0194f

- **Docket ID**: DQAF-20260802-003
- **Date**: 2026-08-02
- **Severity**: Sev 2
- **Title**: btc_swing RR 坍缩 — 对称 SL=TP + spread 不等式 → 结构性负 EV
- **Evidence**: live_launcher_20260802T021000Z.log btc_swing trio rr_ratio=0.9786 (对照 h1_v2=1.2428/h4=1.1977/m30=1.24 均>1.0); live_btc.yaml:413-423 sl/tp.base_atr_mult=1.5/1.5 (对称); BTC_Swing_V4.json label_contract sl=1.5/tp=1.5 (train-serve 对齐); dynamic_sl_tp.py:254-267 spread 不对称施加 (TP 收窄, SL 放宽)。
- **DA**: RR=(d−spread)/(d+spread), spread_cost=200×0.01=$2, d=184.9 → (182.9)/(186.9)=0.9786 精确复现。盈亏平衡 WR=1/(1+0.9786)=50.5% > 所有脑 WR (V4 36-48.5%, ensemble 43.6%, CV 49.8%) → 结构性负 EV → 拦截正确。
- **AR**: 推翻跨周期 ATR (M5/M5, FIX-20260706-027 已修); 推翻"滑点是主因" (spread=$2 是放大器, 对称 1.5/1.5 才是根); 推翻"train-serve 对齐所以没问题" (对齐 ≠ 盈利)。
- **Root Cause**: L3 — 设计致错: 训练/服务合约对称 1.5/1.5 SL=TP 几何, 对 <50.5% 精度模型 (CV 49.8%) 数学无解; strategy_builder 默认 2.0/2.5 与真实配置 1.5/1.5 分叉掩盖设计 (RC-06 config-taxonomy)。
- **Fix**: IC 终局裁决 D+C (维持拦截 + 战略退役, 零代码止血)。FIX-20260802-002 — builder 默认值对齐 SSOT (2.0/2.5→1.5/1.5, 行为中性)。V4 在 observation_hold 过期后 (8/3 23:59:59Z) 由治理依法降级。资本聚焦 H1_V2 (WR 66.7%) + Expected R 双塔。
- **ReB**: SYMMETRIC_SL_TP_PLUS_SPREAD_INEQUALITY; EXPLICIT_BETTER_THAN_IMPLICIT_CONFIG
- **Status**: **CLOSED** — committed 58b0194f

- **Docket ID**: DQAF-20260802-004
- **Date**: 2026-08-02
- **Severity**: Sev 2
- **Title**: bridge_silence 假熔断振荡 — 生命周期探针仅在降级/管理分支盖章, 健康空转路径永不刷新
- **Evidence**: live_cycle.py:382 (L382 契约 "Unix ts of last successful broker.fetch_prices()") / L1207-1208 (一次性初始化) / L1216-1236 (熔断判定) / L1310 (仅熔断分支盖章, FIX-20260608-006); management_phase.py:1305-1306/1311-1312 (持仓门控盖章, live_cycle.py:2708 门控); circuit_breaker_reset.py:65-67 (复位要求 ACK 新鲜); 今日 11:57 全新重启后实证 — BTC 熔断 4 次 (12:19:59/12:54:59/13:29:59/14:09:59, silence 1347-1500s) + XAU 6 次 (12:09:59→14:02:15, ~735s); 周五 07-31 交易时段 20 次; 熔断期间 mt5_bridge_health 心跳新鲜 (14:10:02) + 实时价持续。
- **DA**: 正常路径 broker.fetch_prices() (live_cycle.py:2020) + _mid_and_prices 回退 (L2027-2028) 每周期成功但从不盖章 ACK → _bridge_silence 每周期 +300s 单调增长越 600s → consecutive_degraded>=3 → 假熔断 (management_only) → 熔断分支才盖章 (L1310) → 600s 冷却复位 → 正常路径 ACK 再次冻结 → 无限振荡 (BTC 每 ~35min / XAU 每 ~22.5min, 实盘时间 ~30% 被压制)。
- **AR**: 推翻 H1 "桥真死" (心跳新鲜 + 实时价 100% 假警报); H2 "仅持仓门控" 部分成立但周五 20 次交易时段实证振荡照常; 推翻 H3 "011 已修" (FIX-20260801-013 是 StaleFeatureException 防御响应, 未触碰 ACK 接线 — 011 记录熔断态自我维持半环, 本案卷为健康态从不盖章基座半环); 部分推翻 H4 "假熔断是安全边际" (熔断分支 Fail-Safe Exit Gateway L1273-1330 可市价强平在持仓位, 假警报实际降低安全性)。
- **Root Cause**: L2 — 接线缺陷: 探针被文档化为 fetch_prices 成功即盖章 (L382), 但 4 处 fetch_prices 调用点仅受门控的 2 处盖章, 且全部藏在降级分支后 (RC-06 contract-violation)。FIX-20260608-006 只补熔断分支属不完整修复。
- **Fix**: FIX-20260802-004 (L2 接线, 2 处) — 正常路径 broker.fetch_prices() 成功即盖章 (FaultTolerantContext 异常吞掉不盖章 → 真桥死检测保留) + L3 _mid_and_prices() 回退成功 (mid>0) 即盖章, 镜像 management_phase:1306/1312 模式。验证: verify.py --quick 全 PASS + runtime 定向 331 passed + 熔断器 8 passed (--full pytest 环境性超时, -x 零失败, 如实记录)。
- **ReB**: LIVENESS_PROXY_STAMPED_ONLY_IN_DEGRADED_PATHS (互补 ReB-20260801-LIVENESS_ACK_CIRCULAR_DEPENDENCY — 本案卷为其基座半环)
- **Status**: **CLOSED** — committed 4fada06b

- **Docket ID**: DQAF-20260803-001
- **Date**: 2026-08-03
- **Severity**: Sev 3
- **Title**: MIN_ECONOMIC_VOLUME XAU 定制全局硬编码 — BTC 结构性不可交易 (解除 XAU 霸权 / MIN_ECONOMIC Refactor)
- **Evidence**: 周一开盘 (08-03 00:00Z) 双品种零开单。BTC intent_20260802T180823Z.log cycle 72: h1_v2 LONG conf=0.26 → volume 0.0033, h4 SHORT conf=0.31 → 0.0033, 均 `volume_degraded_below_economic_minimum: 0.0033 < 0.02`; XAU live_launcher_20260802T180824Z.log: m30_swing SHORT conf=0.616 → 0.0066, h1_swing SHORT conf=0.607 → 0.0066, 12-brain consensus 0.866 仍被击杀。strategy_evaluator.py:1144 `_MIN_ECONOMIC_VOLUME = 0.02` 全局唯一 volume 下限, L1141 注释 "For XAU: lot_step=0.01, 2× lot_step = 0.02 minimum economic"。两品种 bridge_health mt5_connected=true, outbox_pending=0, 进程全存活, feature_store age 0.0min。
- **DA**: GodsEye 健康分低下 (BTC 0.327 SHADOW = alignment 0.653 × (1-chop 0.5); XAU 0.438 CAUTIOUS = alignment 0.5 × consensus 1.0) → strategy_evaluator.py:1109 `volume ×= max(0.25, health)` → 成交量坍至 0.0033/0.0066 → 终态闸门 L1145 `volume < 0.02` 击杀。FIX-20260730-010 (Ω Phase 2) 设计 fail-closed: 低信任市况如实报告"条件退化不宜开单"。BTC 结构性缺陷: 配置 base_volume=0.01 (L3 接线层), 标准手 0.01 结构性低于 0.02 下限 → 即使健康分 1.0 也被判不经济 (BTC 历史全部以 0.01 成交)。
- **AR**: 推翻"系统崩溃/挂起" — 双 loop 每 5min 循环 (BTC cycle 72 at 00:00Z), 信号持续产生, dispatch 无 block 标志, outbox 0 积压; 推翻"OU 冷启动锁仓" — 双品种 conformal_calibrator_state 均 500 samples (HOT), force_min_volume=False, 日志零 COLD override; 推翻"桥接断流" — 心跳新鲜 + 实时价持续 (XAU spike health 0.724 at 23:30Z)。
- **Root Cause**: L3 架构异味 — 基于 XAU lot_step 定尺的 0.02 硬编码为全局唯一 volume 下限, 对 BTC (lot_step 0.01, base 0.01) 形成结构性封杀; 单品种过度拟合残留 (Iron Law #14 同族: per-symbol 操作参数未下沉到 symbol registry)。
- **Fix**: IC 终局裁决 (2026-08-03): 🟢 **HOLD THE LINE** — fail-closed 是正确保护行为。🔴 **L3 重构立项**: MIN_ECONOMIC_VOLUME 从全局硬编码重构为 per-symbol / per-strategy-line 可配置。登记 TECH_DEBT-006。**2026-08-03 执行 (IC 最高执行令, TECH_DEBT-006 → Active Sprint P0)**: FIX-20260803-001 — `StrategyLineConfig.min_economic_volume` 字段 + `resolved_min_economic_volume` property (显式配置优先, BTC→base_volume floor 0.01, 其他→2×lot_step 0.02); strategy_builder 20 策略线 `_cfg` 透传 + `_validate_min_economic_floors` 静态跨品种校验; strategy_evaluator 终态闸门改 per-strategy floor (RuleEngine dict-config 防护); live_btc.yaml 6 策略线显式 `min_economic_volume: 0.01`。验证: 60 相关单测全绿 + ruff clean + mypy baseline clean (--full pytest 300s 环境性超时, 定向套件零失败如实记录)。**否决全局降级**: IC 拒绝 0.02→0.01 全局 hack (会拆 XAU 盈亏平衡地板)。
- **ReB**: XAU_CENTRIC_HARDCODED_GLOBAL_THRESHOLD
- **Status**: **CLOSED** — FIX-20260803-001 (L3 重构完成, BTC 结构性封杀解除, 验证通过)

---

- **Docket ID**: DQAF-20260804-001
- **Date**: 2026-08-04
- **Severity**: Sev 1
- **Title**: 治理持久化顺序缺陷 — save BEFORE execute_transitions → 自动 transitions 永不落盘 (V4 IC D+C 退役裁决未执行)
- **Evidence**: gov-eval 日志 1243 次 `transitions=['BTC_Swing_V4: live → probation (throttle)']` (live_launcher.py:263) 但 governance_state.json V4 仍 live/vote_weight=1.0; evaluate_governance_state.py:208-211 save() 在 line 263 execute_transitions() **之前**, 之后无 save; 双部署路径 (container scheduler_service.py:246 + bare-metal live_launcher.py:261) 同调; observation_hold_until 2026-08-03T23:59:59Z 已过期但降级从未生效。
- **DA**: 60s gov-eval 循环: load(disk V4=live) → 注入 brain_performance → **save (V4=live 落盘)** → 内存执行 V4→probation → 返回。下轮重载磁盘 V4=live → throttle 决策每 60s 系统性丢弃。V4 负 EV (35 笔 EV -$0.73/笔, WR 34.3% < breakeven 58.6%) 以 vote_weight=1.0 持续在 live 接线。
- **AR**: 推翻"daily_ops 是真正写者且判定 V4 stable" — gov-eval throttle 决策独立于 pnl 算法, 日志证明 execute_transitions 已应用; 推翻"last-live guard 拦截" — 两个 guard (governance_rule_engine.py:291 / governance_scheduler.py:732) 均不在 gov-eval execute_transitions 路径上; 推翻"文件是旧缓存" — mtime 03:49:11.742 == perf updated_at, V4 仍 live。
- **Root Cause**: L3 — 架构缺陷 (RC-12): 持久化顺序错误 — save 在 Executor 副作用之前, 之后无 save → 内存 transitions 每次重载即丢弃。
- **Fix**: FIX-20260804-001 (L3, commit 73a17820) — 移除前置 save, execute_transitions 之后单次落盘 (manual_mode 分支同覆盖); 双部署路径同享单点. `_verify_governance_evaluator.py` Assert 6 语义翻转为确定性持久化断言 (TestPersist live→probation 落盘重载). 验证: 6/6 断言 VERIFY OK + mypy/ruff clean + 175 governance pytest PASS + pre-commit 全链 PASS.
- **ReB**: PERSISTENCE_BEFORE_EXECUTOR
- **Status**: **CLOSED** — FIX-20260804-001 (commit 73a17820, V4 将在下一 60s 循环依法降级并持久化)

---

- **Docket ID**: DQAF-20260804-002
- **Date**: 2026-08-04
- **Severity**: Sev 2
- **Title**: BTC 跨资产特征零填充 — numpy.void 结构化行误用 + 不存在 API 契约 (slots [12]/[30]/[39-40] 自启动污染特征仓)
- **Evidence**: live_launcher 日志 1600+ 次 `'numpy.void' object has no attribute 'get'` (btc_feature_augmenter.py:639-640 BTC/XAU ratio + 585-586 AUDJPYc) + `'FeatureService' object has no attribute 'get_latest'` (:500 XAUUSDc_return); 特征仓 XAU M5 记录存在 53 条 (数据可用, 代码读不出); live_cycle.py:2558 构造点传 FeatureService (另两处 4042/4903 传 LocalFeatureStore)。
- **DA**: (a) MT5 `copy_rates_from_pos` 返回 numpy 结构化数组, 行是 numpy.void 无 `.get()` → 零填充; (b) `_store.get_latest("XAUUSDc")` API 不存在, FeatureRecord 是 dataclass 非 dict → 零填充; (c) 构造点类型不一致。三缺陷被 except 吞掉 → slots [12]/[30]/[39-40] 持续 0.0 → 污染持久化 v2 特征仓 → 未来重训数据集带系统性零填充。
- **AR**: 推翻"XAU 本就不在 BTC store 是设计" — 53 条 XAU 记录存在 + numpy.void 错误证明 MT5 行情可获取; 推翻"MagicMock 测试证明 API 设计如此" — MagicMock 吸收任意调用, 测试不验证真实 API 形状 (测试盲区)。
- **Root Cause**: L3 — 边界错误 (RC-03): 结构化数组行当 dict 用 `.get()` + 跨符号读取 API 契约缺失 + MagicMock 掩盖测试盲区。
- **Fix**: FIX-20260804-002 (L3, commit ca2c4db9) — 单收敛点: `_coerce_feature_store()` (unwrap FeatureService._store) + `_latest_cross_record()` (latest()+to_dict() 归一 dict) + `_bar_close()` (dict/numpy.void 统一读 close, 负索引归一). 测试契约纠正 (spec=LocalFeatureStore 防 MagicMock 误判) + 3 根因回归锁. 验证: 46 tests PASS + mypy/ruff clean + 296 features/training 全绿 + pre-commit 全链 PASS.
- **ReB**: STRUCTURED_ARRAY_ROW_AS_DICT / API_CONTRACT_MAGICMOCK_BLINDSPOT
- **Status**: **CLOSED** — FIX-20260804-002 (commit ca2c4db9, 零填充停止, 新 v2 特征记录纯净)

---

- **Docket ID**: DQAF-20260804-006
- **Date**: 2026-08-04
- **Severity**: Sev 2
- **Title**: XAU 进场位置系统性最差 — 100% SHORT 偏置 + 模型输出退化 + D1 特征毒井
- **Evidence**: `data/live_trade_journal.jsonl` 今日 3 单全 SHORT 全在局部底部 (m15 4048.7@10:15 / m30 4049.1@10:15 / h1 4063.8@12:10), 2 SL + 1 深套 (上涨日 4047→4082); `data/brain_votes/2026-08-04.jsonl` H1_Exec_A 155/155 SHORT + M30_V5 154/155 SHORT (script 输出: scripts/audits/_audit_xau_votes_today.py); 信号方向 886 SHORT:3 LONG; 退化签名 M15_V7_binary conf_uniq=1 恒定 0.783; **D1 毒井**: `data/raw/xauusdc_d1_merged.csv` 行 2510-2532 (2026-07-04→08-04) 价格 63,000-64,700 = BTC 量级。
- **DA**: 全 swing 脑 D1_* 特征 (D1_Ret_1/ATR/RSI/MACD/Vol_ZScore/Bollinger/ADX) 吃进 BTC 价 → out-of-distribution → 输出退化 SHORT 锁死。执行无滑点 (信号 10:15:02→执行 10:15:22, 价差<0.3) — 非执行问题, 纯信号方向问题。
- **AR**: 推翻"滑点/延迟" (20s 延迟价差<0.3); 推翻"方向映射反转" (brain_votes raw_score 符号验证正确); 推翻"今日 FIX/重启回归" (3 单在 12:58Z 重启前, FIX 全 BTC 侧); 部分成立"顺势追突破"但方向反 (886:3 结构性反向)。
- **Root Cause**: L2 — 模型输出退化 (MODEL_OUTPUT_DEGENERACY_SHORT_COLLAPSE, 同签名 FIX-20260629-184) + **上游 L3 毒井** (RC-09 config-drift: `live_intent_loop.py:759` H1 硬编码 XAU D1 CSV → BTC 进程 LiveDailyProvider 跨写 → 07-04 起 D1 特征全毒)。
- **Fix**: FIX-20260804-006 — 6 脑物理冻结 (retired + vote_weight 0: M30_V5/H1_Exec_A/M30_Exec_A/M15_V7_binary/H1_V2_binary/M30_V7_binary) + live.yaml 3 条接线 enabled:false. 三段全部落地: (1) FIX-20260804-007 live_bootstrap D1/H4 per-symbol 参数化 + Symbol Guard; (2) FIX-20260804-007 毒井 CSV 截断 (2522→2510, 备份保留) + 重启回填; (3) FIX-20260804-008 monitor 三重修复 (全资产/嵌套字段/大小写)。重启 143126Z/143213Z 生效 (冻结 6 脑 fully muted)。
- **ReB**: MODEL_OUTPUT_DEGENERACY_SHORT_COLLAPSE / D1_WELL_CROSS_ASSET_POISONING / MONITOR_ASSET_HARDCODED_DATA_DIR
- **Status**: **CLOSED** — FIX-20260804-006/007/008 全链落地 (freeze + well-cleansing + monitor wake; 用户重启 143213Z 生效; monitor 实证 XAU CRITICAL 86% SHORT / 100% trades)

---

- **Docket ID**: DQAF-20260804-007
- **Date**: 2026-08-04
- **Severity**: Sev 2
- **Title**: D1 特征毒井 — BTC 数据污染 XAU D1 管线 (跨资产写)
- **Evidence**: `data/raw/xauusdc_d1_merged.csv` 行 2510-2521 尾部 12 行 (2026-07-04→08-04) 价格 63,093-64,794 = BTC 量级; 行 2508-2509 (07-26/27 XAU 4093-4094) 与毒行交错证明 date-keyed dedup 阻断 XAU 正确回填; `live_intent_loop.py` init_feature_services 硬编码 `d1_csv="data/raw/xauusdc_d1_merged.csv"` (BTC 进程同路径构造 LiveDailyProvider, `_sync_csv` 按 symbol=BTCUSDc 拉 BTC bar 追加进 XAU 文件)。
- **DA**: BTC 进程 `LiveDailyProvider._sync_csv()` 无条件追加 → 无符号守卫 → 每新 D1 bar 跨写; 毒化自 07-04 起与 SHORT 偏置升级期完全重合。
- **AR**: 推翻"XAU 自身写错" (XAU 进程写 XAU bar 正确, 污染源是 BTC 进程); 推翻"只毒一次" (每次新 bar 重复追加)。
- **Root Cause**: L3 — RC-03 边界错误/硬编码: 跨资产共享 XAU CSV 路径, `LiveDailyProvider` 无 symbol↔filename 契约守卫。
- **Fix**: FIX-20260804-007 — (1) `live_bootstrap.init_feature_services` D1/H4 CSV 按 `_asset_base_dir/raw/{symbol}.lower()_d1_merged.csv` 派生 (XAU→data/, BTC→data_btc/); (2) `LiveDailyProvider._assert_d1_symbol_contract()` 构造时 + `_sync_csv` 写前熔断 (DataIntegrityError 不被 FOG 吞, bootstrap re-raise); (3) 毒尾截断 2522→2510 + 全量备份 `_quarantine_xauusdc_d1_merged_poisoned_20260804.csv`; 重启后 XAU init 自动回填正确 bar. +5 单测。
- **ReB**: D1_WELL_CROSS_ASSET_POISONING
- **Status**: **CLOSED** — FIX-20260804-007 (code + 截断 + 备份完成, 待重启部署生效)

---

- **Docket ID**: DQAF-20260804-008
- **Date**: 2026-08-04
- **Severity**: Sev 2
- **Title**: direction_concentration_monitor 三重结构性失聪 — 99.7% SHORT 退化零告警
- **Evidence**: 今日 XAU 3 单全 SHORT 无任何告警; `scripts/_monitor_direction_concentration.py` `_scheduled_monitor` 硬编码 `data_dir="data_btc"` (XAU 永不检查); 实测 golden_master 时间过滤 0 行 (字段 `timestamp_utc` vs 读 `timestamp`/`recorded_at`) → 永远 INSUFFICIENT DATA; 方向读顶层 `direction` (恒 None) vs 实际 `outputs.<strategy>.direction`; 大小写错配 (gm 小写 short/long vs 匹配大写 SHORT)。
- **DA**: 监控器对 XAU 完全静默 → 退化脑 SHORT 锁死一个月无风控警报; 风控警报器失效比模型失效更不可饶恕 (IC 语)。
- **AR**: 推翻"数据不足" (修复后实测 24h 577 信号); 推翻"XAU 无退化" (修复后实测 86% SHORT / 100% trades)。
- **Root Cause**: L2 — 监控器读错字段/目录/大小写三重盲区 (MONITOR_FIELD_PATH_MISMATCH), 恒 INSUFFICIENT DATA 静默。
- **Fix**: FIX-20260804-008 — `DEFAULT_ASSET_DIRS=("data","data_btc")` 全资产扫描 + worst-wins 聚合 + alert 带 asset_dir; `_extract_gm_direction` 嵌套 outputs 提取; `normalize_direction` 大小写+BUY/SELL 归一; gm 时间过滤 `timestamp_utc`; GBK 控制台 emoji 崩溃修复. 实证: XAU CRITICAL (86% SHORT 信号 496/577, 100% trades 50/50), BTC 平衡. +9 单测。
- **ReB**: MONITOR_FIELD_PATH_MISMATCH / D1_WELL_CROSS_ASSET_POISONING
- **Status**: **CLOSED** — FIX-20260804-008 (实证检测 CRITICAL, 修复完成)

- **Docket ID**: DQAF-20260807-002
- **Date**: 2026-08-07
- **Severity**: Sev 1
- **Title**: 系统性高位接盘/低位追空 — 趋势追家族空间维度裸奔 (TREND_CHASE_NO_POSITION_GATE)
- **Evidence**: Iron Law #11 审计脚本 (scripts/_audit_entry_timing_20260807.py + scripts/_audit_entry_regime_20260807.py, 留工作树): 全历史 640 笔 — 多头平均 H1_z=+0.034 (略偏上沿); LONG H1_z>+1.5 桶 = 25.7% 胜率 / **−49.29 总盈亏 (全表最差桶)**; LONG H1_z>+1.0 桶 = 25.7% / −49.29 (同源); SHORT H1_z<−1.0 桶 = 44.3% / **+99.36 (盈利)**; 用户实盘观察 (震荡区最高点开多/最低点开空) 与全历史数据一致.
- **DA**: 趋势追家族 6 策略 (m5/m15/m30/h1/h4/daily_swing) 全部空间守卫 (4aa 方向感知 / 4b 多TF硬过滤 / 4c counter-trend ADX / 4d z 拐点) 均为方向闸, 零价格位置闸. check_z_inflection 被 `"statarb" in name or "ou" in name.lower()` 硬限制 (trend_isolation_gates.py:205-207) → swing 家族唯一的价格位置校验不可达. ML 动量特征在区间极值处峰化 → 结构上追多/追空.
- **AR**: "仅三单偶然" → 被全历史 640 笔分布推翻 (long avg z=+0.034 系统性); "Short 低位追空同样有害应同样硬断" → 被 SHORT z<−1.0 桶 +99.36 盈利数据推翻 (非对称设计: Long 硬断, Short 仅降额); "fail-open 会误放行" → 设计契约: 数据缺失/非有限 → 放行 (不制造新阻塞), 仅在有效数据时收紧.
- **Root Cause**: L3 (RC-06 architecture-incomplete) — 信号生成层只有时间维度方向保护, 空间维度价格位置零守卫; z 拐点基础设施存在但被策略白名单排除 swing 家族.
- **Fix**: FIX-20260807-002 — trend_isolation_gates.py 新增 `apply_spatial_zscore_gate` (4e): LONG H1_z>+1.5 (ranging/chop 收紧 +1.0) → SpatialGateResult(blocked=True) 硬否决 (should_trade=False/volume=0); SHORT H1_z<−1.5 (ranging −1.0) → volume_mult=0.5 降额; config 可覆盖阈值 (同 4c ADX 模式). `extract_h1_price_zscore` 走 schema registry 动态索引 (v9_institutional_40 H1_Price_ZScore, 非硬编码索引位). strategy_line.py counter-trend 动作块后接线 (避开 `_ct_vol_mult` 覆写) + spatial_zscore_gate JSON 事件. 回归锁 19 测试.
- **ReB**: TREND_CHASE_NO_POSITION_GATE
- **Status**: **CLOSED** — FIX-20260807-002 (IC 雷霆裁决 Option A+C, 8/19 前禁止 Option B)

- **Docket ID**: DQAF-20260817-001
- **Date**: 2026-08-17
- **Severity**: Sev 2
- **Title**: XAU TP 持仓中大幅缩窄、SL 不动 → 运行中 RR 坍缩 (TP_TRAIL_RR_COLLAPSE_DECOUPLED_FROM_SL)
- **Evidence**: Iron Law #11 探针 (scripts/_audit_xau_tp_shrink_20260817.py, 留工作树): 8/17 journal + position_snapshots — 4500875936 (h1_swing SHORT) TP 84.3→25.7 点 (02:35 label="trail"/comment="tp"), RR 1.73→**0.527**, SL 4451.41 全程未动; 4501482790 (m15_swing SHORT) TP 34.4→13.5 点, RR 0.98→**0.385**. 两策略 min_rr_ratio=0.85 (configs/live.yaml) 均跌破. 盈利曾达 R=+2.108 (4500875936).
- **DA**: TP trailing (FIX-20260713-008, position_manager.py:1707 compute_trail_tp) 在 atr_ratio=current_atr/entry_atr ≤0.80 时把 TP 向内收窄 (candidate = anchor ∓ trail_mult×current_atr×1.75×_tf_scale) 且只缩不放; 缩窄下限与 SL 距离/RR 零耦合 → RR 坍缩.
- **AR**: "SL 也应移动只是未触发" → 被推反 (Chandelier trail_activation_atr h1=0.7/m15=0.4 未达, 快照 trail_fired=false); "TP 缩窄合理 (锁利润)" → 被 RR<0.85 负期望推翻 (止盈空间<止损空间); "仅今日偶然" → FIX-20260709-004 前案 (RR 1.66→0.08) 证明系统性复发.
- **Root Cause**: L3 (architecture-incomplete) — TP Floor (tp_min_distance_atr×bracket_atr, max()/min() 语义) 是 upper bound 防"太激进(太远)", 非 lower bound 保 RR; Proximity Gate 仅防末程; bracket inversion guard 仅防 TP 穿 SL 不防 RR<1; 只缩不放.
- **Fix (缓解)**: 投委会 2026-08-17 行动令 — 物理冻结 m15/m30/h1_swing (configs/live.yaml enabled:false, commit 09f09ea2) 切断新开仓; **TECH_DEBT-019** 建档 (三条修复蓝图 8/19 后清偿: ①RR 硬底线 max(Dynamic_TP_dist, Current_SL_dist×min_rr_ratio) ②波动率对称耦合 ③弹性恢复). 在仓 2 单继续 trail/watchdog 管理至平仓. 根因修复 Deferred (8/19 决战冻结期零代码).
- **ReB**: TP_TRAIL_RR_COLLAPSE_DECOUPLED_FROM_SL
- **Status**: **CLOSED (mitigated)** — 冻结缓解已落地 + TECH_DEBT-019 根因修复 Deferred 至 8/19 后

- **Docket ID**: DQAF-20260819-003
- **Date**: 2026-08-19
- **Severity**: Sev 2
- **Title**: TECH_DEBT-008 清偿 — zombie-fuse 熔断告警静默 (LIVE_ALERT_HUB_SIG_DRIFT / FROZEN_DEBT_MASKING_LIVE_BUG)
- **Evidence**: 8 处 RED_LINE_FROZEN_ALLOWANCE unified 错误, `python -m mypy --follow-imports=normal` 精确复现. live_intent_loop.py:2413-2431 zombie-fuse 告警块三重错配: `_ah = getattr(state, "_alert_hub", None)` 恒 None (全模块零 `_alert_hub =` 赋值) → fallback `LiveAlertHub(log_dir=f"{config.base_dir}/logs", ding_webhook_url=...)` — `log_dir`/`ding_webhook_url` 参数不存在 (实际签名 `base_dir`/`symbol`/`dingtalk_url`/`dingtalk_secret`) → TypeError; `.fire("zombie_cycle_fuse_blown", ...)` 方法不存在 (现行 `send_critical(reason, detail)`) → AttributeError; 整块被 `except (RuntimeError, ValueError, KeyError, TypeError, OSError): pass` (BLE001:FOG) 吞掉 → 熔断信号从未送达, 只剩本地 watchdog_kill.log + `_os_module._exit(3)`.
- **DA**: zombie-fuse 是引擎死循环的**最后生存信号** — 静默化 = 运维在引擎已死时毫不知情 (仅本地 log); 8 处类型债中 6 处纯声明/调用点 (L1, 零行为风险), 危险分子是 zombie-fuse 行为级缺陷 (L2).
- **AR**: "type: ignore 压制即可" → 被推翻 (告警 bug 是运行时 TypeError/AttributeError 非类型问题, 压制只会让静默永久化); "state._alert_hub 恒 None 是设计" → 被推翻 (全模块 grep 零赋值, 从无此状态字段, 每 cycle fallback 构造必 TypeError).
- **Root Cause**: L2 (RC-06 contract-violation) — LiveAlertHub 接口演进后 zombie-fuse 调用点未迁移 (构造参数 + 方法 + state 字段三错配), 叠加 BLE001 吞异常 → 熔断告警全静默. 其余 7 处 L1 — 类型声明与实现不一致 / 调用处未传必参 / 变量名复用 (market_ingress ×2 / live_cycle ×2 / shadow_ensemble ×1 / governance_scheduler ×1).
- **Fix**: FIX-20260819-003 — zombie-fuse 块改 `_ah = alert_hub or getattr(state, "_alert_hub", None)` (main 局部变量 args.alert=True 时带真实 webhook) → None 时 fallback `LiveAlertHub(base_dir=config.base_dir, symbol=<args.symbol 或 base_dir 推导>, dingtalk_url=args.dingtalk_webhook or "", dingtalk_secret=args.dingtalk_secret or "")` + `_ah.send_critical("zombie_cycle_fuse_blown", detail={consecutive_errors/last_error_type/last_error/last_traceback/cycle_count})`; 保留 BLE001:FOG (fuse 触发不阻断 exit). 其余 7 处: market_ingress `_compute_atr_from_rates` 签名补 `| None` (实现已 None-guard) ×2; live_cycle feature_vector_sample 调用处 `list()` 转换 + `DataHealthService(base_dir, symbol, mode="light")` ×2; live_shadow_ensemble cross_assets 去 `str()` 注解 `dict[str, str | Path]` ×1; governance_scheduler `_jm` → `_leaked_metrics` ×1. **RED_LINE_FROZEN_ALLOWANCE 全数删除** + `pre_commit_mypy.py --update-baseline` 重生成 (仅剩 test_tech_debt_010 隔离模式 unused-ignore 伪差 1, 非新债). 回归锁 3 (tests/deployment/test_tech_debt_008_alert_hub_contract.py).
- **ReB**: FROZEN_DEBT_MASKING_LIVE_BUG / LIVE_ALERT_HUB_SIG_DRIFT
- **Status**: **CLOSED** — FIX-20260819-003 (8 处根因清除, mypy unified EXIT=0, 针对性回归 296 + 新锁 3 全绿)

- **Docket ID**: DQAF-20260819-004
- **Date**: 2026-08-19
- **Severity**: Sev 2
- **Title**: TECH_DEBT-017 清偿 — FaultTolerantContext 降级路径作用域陷阱 (FTC_SCOPE_TRAP_UNBOUNDLOCAL)
- **Evidence**: 8/11→8/13 休市期 intent_loop 每天 11 连崩共 38 次 — TECH_DEBT_REGISTRY.md L212-227 记录完整 traceback. 两处 UnboundLocalError: (1) live_cycle.py startup_reconciliation `_positions` — mt5_call_with_timeout 抛 RuntimeError (MT5 离线/休市), FTC(DEGRADE) 吞异常继续 → `_positions` 在块内 RHS 抛出时永不绑定 → 块外 `set(state.known_open_tickets.keys()) - _open_tickets` 访问 _open_tickets→_positions 崩; (2) live_intent_loop.py while 循环中部赋值 `_EVENT_STREAM_MODE = True` (原 L2553), 异常跳转路径 (DEGRADE/except) 可跳过 → L2743 `if not _EVENT_STREAM_MODE:` UnboundLocalError. UnboundLocalError 继承 NameError, 不在 live_cycle/live_intent_loop except 元组 (RuntimeError, ValueError, KeyError, TypeError, OSError) 内 → 穿透崩溃 → launcher 5s→30s respawn 无限循环. 系统扫描另发现 2 处同类潜伏缺陷: live_cycle PnL_to_equity `_eq` + group_consensus CorrelationTracker:penalty `dynamic_volume`.
- **DA**: FTC 五级故障分类 (CRASH re-raise / DEGRADE/LOG/IGNORE 吞异常继续) — 吞异常路径必须假设块内变量可能永不绑定. fault_handler.py docstring L141-158 官方自述: "When the block body contains an assignment and the right-hand side raises, the variable name is never bound in the local namespace — even though FTC swallows the exception. Always pre-initialise variables before the `with` block." 但调用点未系统性落实 → 缺陷点 (live_cycle L1509/1843, group_consensus L123) vs 安全点 (live_cycle L2039/3520/4717/4921, management_phase L1060/1372/1517/527, group_consensus L64 均已预绑定).
- **AR**: "休市崩溃只是 MT5 离线噪音可忽略" → 被推翻 (38 次崩溃每天 11 连崩 = 引擎无法存活完整休市周期, 恢复瞬间状态重置 + launcher 风暴; 且 4 处含生产路径 PnL 折算); "加 UnboundLocalError 进 except 元组即可" → 被推翻 (吞异常后 _positions 仍为 None, reconciliation 逻辑与注释矛盾 — 需显式降级语义, 非 merely 捕获).
- **Root Cause**: L3 (architecture-incomplete) — FTC(DEGRADE/LOG/IGNORE) 契约"吞异常继续执行" × Python 作用域语义"块内 RHS 抛出则变量永不绑定"冲突; fault_handler.py docstring 自述但无机制强制调用点预绑定 → 每个 DEGRADE 块都是潜在 UnboundLocalError 地雷.
- **Fix**: FIX-20260819-004 — Scope-Safe Pre-binding 4 处: (1) live_cycle `_positions: list[Any] | None = None` 预绑定 + `_skip_recon` 布尔守卫 (DEGRADE/MT5 超时 → 跳过 reconciliation, known_open_tickets 保留不丢持仓跟踪, 统一 L1518 注释本意); (2) live_cycle `_eq: float = 0.0` (DEGRADE 时 pnl_pct=0); (3) live_intent_loop `while True` 循环体最顶层 `_EVENT_STREAM_MODE = True` 前置绑定 (中部赋值保留幂等); (4) group_consensus `dynamic_volume = raw_volume` (无惩罚回落). 回归锁 tests/runtime/test_tech_debt_017_scope_safety.py 5 测试 (1 behavioral DEGRADE 吞 RuntimeError → dynamic_volume==raw_volume + 4 static 预绑定先于 FTC 块顺序断言防回归).
- **ReB**: FTC_SCOPE_TRAP_UNBOUNDLOCAL
- **Status**: **CLOSED** — FIX-20260819-004 (4 处根因清除, mypy/ruff PASS, verify --quick PASS, 针对性回归 47 + tests/runtime+parliament 399 + 新锁 5 全绿)
